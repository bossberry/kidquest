# CHATBOT_NOTES.md — Phase 1: Friend Code System

## Claude Code Handoff
_(Claude Code appends here after each session)_

**2026-07-05 — Shop polish: renamed "ห้องแต่งตัว", icon-only item cards, bigger slot switcher:**
- Context: quick feedback-driven polish on the dressing-room redesign — user said "Use Sonnet" (small, well-scoped, matches everyday-task guidance), implemented directly.
- The trickiest part was making item cards icon-only (no egg underneath) without touching `eggCosmeticLayer.js` — read its top-of-file docs carefully first and found each cosmetic's `draw()` function already only draws the item itself (an 18×18-cell egg-relative coordinate convention, documented explicitly), never the egg body/eyes. So calling it directly with a synthetic `{px,ox,oy,faceX}` frame (no real egg involved at all) was all that was needed — derived the frame's scale/offset from actually reading several draw functions' real y-ranges (head items reach y≈-10 for tall hats, face items reach y≈+11) rather than guessing, so nothing clips.
- Built: header title "ห้องแต่งตัว" added (there wasn't one before), `BottomNav.jsx`'s "ร้านค้า" label → "แต่งตัว", new `ItemIcon` component, bigger cards (96px) with 6-char-truncated bold names, repositioned badges (✓/🔒 top-right, price bottom-right, dark overlay over just the icon when unaffordable — not the whole card), bigger slot switcher with white active-tab text.
- Verified: `npm run build` clean, live-tested with the test account — checked every item in both head/face tabs rendered correctly with zero egg visible, specifically zoomed in on the two trickiest scale extremes (the tiny bow accessory and the compact gold crown) to confirm they still read recognizably at the shared icon scale.
- Not finished: nothing in scope.
- Blockers/risks found: none.
- Ready to start next: Phase 4 NPC System; content expansion; optional Room polish (desk/chair iso faces, flagged many sessions ago).
- Needs Chatbot decision first: nothing blocking.

**2026-07-05 — Fix: maze portal missing + world map colors brightened:**
- Context: two world-map bug reports — the maze portal (ประตูมิติ) was reported missing, and map colors looked dull/grey. User said "Use Sonnet" this time (small, well-scoped fix, matches `CLAUDE.md`'s guidance) — implemented directly, no subagent.
- Root cause on the portal, found by reading the code before touching anything: `drawMazePortal()` sized itself off a stale local `TILE=16` constant left over from the pre-Pandora flat renderer, while its caller (`useWorldGameLoop.js`) had already moved to `PANDORA_TILE=32` for positioning and was passing an offset assuming the callee matched — it didn't. Ran a quick standalone calculation (not just code-reading) to confirm the exact error before editing: the portal was rendering ~8-16px off-position and at half its correct radius. This confirmed the portal was never removed from map generation (the spawn logic in `WorldScreen.jsx` is untouched and correct) — it was just rendering too small/misplaced to notice, exactly the "tile exists but isn't rendering visually" diagnosis from the user's own prompt.
- Built: `drawMazePortal(ctx, cx, groundY, frame)` now takes an already-centered point (matching `drawPandoraChest`/`drawPandoraPlayerGlow`'s convention) and scales off `PANDORA_TILE`; both call sites updated to pass the true center directly. Made it more visually prominent too (soft bloom, bigger rings, brighter core, ground shadow, 4 sparkles instead of 3) per the "make it unmissable" ask. Removed the now-dead `TILE=16` constant.
- Brightened/warmed the ground/path/tree palette in `tileEngine.js` (grass/path/tall-grass bases + their derived dot/hilite/line shades moved together so the texture stays cohesive; tree canopy range widened+brightened to the exact `#3a9a22–#5abf30` the user specified) and added a faint warm ambient wash composited over the whole frame at the end of `renderMapPandora()`.
- Verified: `npm run build` clean. Live-checked the color fix directly on the real running map with the test account — visibly warmer/brighter. For the portal, rather than hoping the test account happened to have an active maze portal on the current screen, verified `drawMazePortal`'s corrected behavior via a dynamic-import test harness (rendered it standalone on a bare canvas at a known center point, screenshot-confirmed exact centering + a clear, distinctive swirl) — a reusable technique for verifying canvas-drawing fixes when the real trigger condition isn't easy to force.
- Not finished: nothing in scope.
- Blockers/risks found: none.
- Ready to start next: Phase 4 NPC System; content expansion; optional Room polish (desk/chair iso faces, flagged many sessions ago now).
- Needs Chatbot decision first: nothing blocking.

**2026-07-04 — Fix critical bug: furniture unbuyable anywhere (regression from yesterday's redesigns):**
- Context: urgent bug report — furniture couldn't be bought anywhere in the app. Root cause: the Shop redesign (2026-07-03) removed the furniture tab assuming `Room.jsx` would sell furniture directly, but `Room.jsx`'s locked-item tap only ever showed a "go to the shop" toast — pointing at a tab that no longer existed. This is a real gap between two redesigns done back-to-back yesterday that neither individually would have caught (Shop's own review confirmed furniture-buying logic was gone; Room's own review at the time didn't flag that its "go to shop" toast would become a dead end once Shop's furniture tab was removed).
- Built: `Room.jsx`'s locked-item tap now opens a small in-sheet buy-confirm view (price, buy button) instead of a toast; buying dispatches `BUY_ROOM_ITEM` then immediately `PLACE_ROOM_ITEM` for the open slot, so the child buys and places in one flow, reusing the existing `handlePlace` feedback (sparkle/bounce/toast/close). Confirmed the dispatch-ordering is safe (React applies queued dispatches against updated state in sequence, so `PLACE_ROOM_ITEM`'s ownership check already sees the just-bought item) before relying on it. Removed the now-dead `shakeItemId` state.
- This was implemented directly (not delegated to Opus) — small, urgent, well-scoped fix, consistent with `CLAUDE.md`'s "prefer Sonnet for everyday tasks."
- Live-verified with the test account before pushing, given how urgent/embarrassing this bug is: bought a locked chair for real, confirmed exact coin deduction and correct placement.
- Not finished: nothing in scope. Worth flagging as a process lesson: when two redesigns each remove/relocate a feature assuming the OTHER one now handles it, it's worth explicitly cross-checking the seam between them (in this case, "Room's toast says go to Shop" + "Shop no longer has that tab" should have been caught during the Shop redesign's own review, since I had literally just reviewed Room's locked-item behavior a few tasks earlier in the same session).
- Ready to start next: Phase 4 NPC System; content expansion; optional Room polish (desk/chair iso faces, flagged multiple sessions ago).
- Needs Chatbot decision first: nothing blocking.

**2026-07-03 — Home screen: full-screen walking room, floating action buttons:**
- Context: last of a run of "Use Opus" redesign requests this session (Room, Shop, now Home). Request: room fills the whole Home screen, egg walks around and reacts to taps, all action buttons become floating icons on the room instead of rows below it.
- Before delegating, I read `Home.jsx`/`DecoratedRoom.jsx`/`roomScene.js` myself and found the spec rested on two things that weren't true: (1) it assumed the room's walker already wandered the iso floor — it didn't, it was a leftover flat left-right-bouncer from the pre-iso room (exactly why Home has always disabled it via `showWalker={false}` and used a separate static egg overlay instead); genuinely new iso-aware movement logic was needed. (2) it assumed `DecoratedRoom` needed a `mode: 'home'|'edit'` prop so `Room.jsx` wouldn't regress — I grepped every importer of `DecoratedRoom` and found exactly one, `Home.jsx`; `Room.jsx` has its own separate canvas and has never touched `DecoratedRoom` at all, so there was no such compatibility constraint to design around. Both corrections went into the brief, along with a pointer to reuse `Room.jsx`'s own `hitTestZone`/`slotCenter` for tap-to-floor-point conversion instead of having the agent invent inverse iso-projection math from scratch.
- Built: `DecoratedRoom.jsx` rewritten with real iso tile-space wander targets, real tap hit-testing (near-egg → pet callback, elsewhere-on-floor → walk there via the reused Room.jsx functions), an imperative `apiRef.walkToScreen()` for item-button summons, and a live-position "follow layer" (DOM transform, zero React re-renders) so reaction/particle overlays track the moving egg instead of assuming a fixed screen-center. `Home.jsx` rewritten: room now fills the whole screen (`BOTTOM_NAV_H` convention, same as this session's Room/Shop redesigns — I also added this convention as a proper `CLAUDE.md` "Always Do" bullet since it's now an established pattern used in 3 screens), header/status bar became translucent floating overlays, and the minigame card / item tray / explore button rows are gone in favor of floating circular buttons that call the exact same unchanged underlying functions/hooks.
- The implementing agent used a temporary login-bypass in `App.jsx` to drive the real app for its own testing, and its cleanup (killing dev-server processes) took down my own running dev server too — flagged clearly in its report, I verified the `App.jsx` revert was actually clean via `git diff --stat` (zero changes) before trusting it, then just restarted the dev server myself. Worth knowing this can happen if a future agent does the same kind of in-browser self-testing.
- **Live-tested end-to-end with the test account** (now three redesigns in a row this session verified this way, not just code review): tap-far-on-floor walked the egg to the exact tapped tile; tap-near-egg visibly changed its expression (pet firing); tapping the food button showed the armed highlight AND the egg visibly trotting toward the button; tapping again used it (count badge 2→1, armed state cleared correctly); minigame button's live lives-badge rendered; explore button navigated to the world map and back cleanly. Zero console errors throughout.
- Not finished: nothing in the spec's 6 sections — the agent reported no descoping was needed (all four priority items plus both optional flourishes shipped).
- Blockers/risks found: none remaining. Worth a note: this was the most technically novel piece of work this session (genuine new movement AI didn't exist in this codebase before today) — if anything about the wander behavior feels off during real play (Chopin testing), it's a newer, less-battle-tested system than everything else touched this session.
- Ready to start next: Phase 4 NPC System; content expansion; optional Room polish (desk/chair iso faces, flagged several sessions ago now).
- Needs Chatbot decision first: nothing blocking. This closes out the string of redesign requests from today — worth a decision on what's next given Room/Shop/Home have all had major UX passes today.

**2026-07-03 — Shop redesigned as a full-screen "dressing room"; furniture tab removed:**
- Context: same session as the Room UX redesign and the cross-device sync fix — user asked for the Shop to become a visual-first "dressing room" (remove the furniture tab since Room now sells its own furniture, canvas-drawn background scene, tap-to-preview item grid, single buy/equip CTA button), again with "Use Opus."
- Before delegating, read `Collection.jsx`/`EggCanvas.jsx`/`eggCosmeticLayer.js` myself and caught two things the literal spec got wrong: (1) real item names run up to 11 Thai characters (มงกุฎอัญมณี, ที่คาดผมดาว) — the spec's "truncate to 4 chars" would have mangled several into nonsense, so I told the agent to use real CSS ellipsis instead; (2) the spec's ✕ close button had nowhere to navigate to, since `Collection` receives zero props from `App.jsx` today — had the agent thread a `navigate` prop through properly rather than build a dead button.
- Built: furniture tab/section fully deleted from `Collection.jsx` (confirmed via grep — zero references left; `roomItems.js`/`ownedRoomItems`/`BUY_ROOM_ITEM` untouched, only the Shop's UI for them removed). New dressing-room scene (canvas-drawn mirror/wardrobe/vanity-bulbs/sparkles/floor, no images), egg zone + slot switcher + 3-column item grid + single floating CTA button that's now the only way to equip/buy (tapping a card just selects/previews — replaces the old inconsistent tap-to-toggle-if-owned behavior).
- **First Shop-area feature live-tested end-to-end with the test account, not just code review** — logged in fresh, opened the dressing room, confirmed the scene renders, tapped an unowned item and watched it preview live on the egg, tapped the CTA and confirmed a real purchase (10010→9965 coins, exactly right; ✓ badge appeared; egg genuinely wearing it), switched slots, confirmed close returns to Home with the item visible on the room walker too.
- **Found and fixed a real bug live testing caught that code review missed**: on this browser's viewport height, the floating CTA button hard-overlapped a row of item cards on initial load — everything was still reachable by scrolling, but the flat cutoff looked like a rendering glitch to a first-time viewer. Added a small fade-gradient above the CTA so scrolled content fades out instead of hard-cutting. This is exactly the kind of thing that's invisible in a code diff and only shows up when you actually click through the real UI — reinforces how valuable the test account has been today (this is the third feature in a row it's caught something for).
- Also corrected two stale `CURRENT_STATE.md` blocks found while updating docs (unrelated to this session's actual code changes, just noticed while I was in there): a leftover furniture-tab mention in the Room section, and an entire "Collection Screen" doc block that had been describing a pre-companion-migration version of the screen (party tabs, ItemBag, `CreatureDetailPopup.jsx`) for a long time.
- Not finished: nothing in the spec's 5 sections.
- Blockers/risks found: none.
- Ready to start next: Phase 4 NPC System; content expansion; optional Room polish (desk/chair iso faces, flagged several sessions ago) — though note a Home screen redesign request (iso room fills the whole Home screen, egg wanders/runs to tap, floating action buttons) arrived immediately after this task and is likely already in progress or done by the time this is read.
- Needs Chatbot decision first: nothing blocking.

**2026-07-03 — Fix critical cross-device sync data-loss bug (+ added a test account to close the recurring live-testing gap):**
- Context: earlier the same day I did a report-only investigation of a bug report (mobile has correct data, desktop shows wrong/empty) and traced it to three compounding issues in `resolveSync`/`saveState`/`defaultState` — no code changed at that point, just a report. Later the user came back with their own 4-step fix spec and said "Use Opus" (same pattern as the icon-first and Room redesign tasks). This entry is the actual fix.
- **Before delegating, I found a real gap in the user's spec**: their fix would correctly protect a literally-brand-new account, but the mount effect's `DECAY_HAPPINESS`/`CHECK_DAILY_RESET`/`DAILY_LOGIN` dispatches all stamp `lastSavedAt: Date.now()` in-memory *before* the async cloud fetch resolves — so a purely timestamp-based fix would still lose the race for a *returning* player on a new device (the actual realistic trigger of this bug), not just a blank account. I traced this myself by reading the mount effect line-by-line, then briefed the implementing agent with this as a REQUIRED fix, not just the user's literal 4 steps, and told it to verify my reasoning independently rather than trust it.
- Built: `defaultState().lastSavedAt` → `0` (was `Date.now()`); new `_initialSyncComplete` flag blocking `saveState()` entirely until the initial `loadState()`→`resolveSync()`→`INIT` chain resolves (with a new `.catch()`/`.finally()` so a fetch failure never permanently disables saving); `resolveSync()` gained two new early-return safety nets before its untouched timestamp fallback; both `saveState()` and `syncToSupabase()` independently refuse to persist an unstamped state with no real progress.
- **The agent's solution to the timing gap was better than either option I suggested** (reorder the mount effect, or snapshot pristine state) — it built a new `hasRealProgress()` check deliberately restricted to fields the maintenance dispatches never touch (`hatchedEggs`/xp/`rounds`/`ownedItems`/`ownedRoomItems`/`grade`/`badges`, excluding `coins`/`happiness`/login fields), which closes the gap without regressing the intentional same-device daily-login-coins-win-the-tiebreak behavior that my two suggested approaches would have broken. I verified this claim myself rather than trusting the report — grepped the actual `DECAY_HAPPINESS`/`CHECK_DAILY_RESET`/`DAILY_LOGIN` reducer cases in `StateContext.jsx` and confirmed none of them write to any field `hasRealProgress()` tracks.
- **First live end-to-end test of a fix in this problem area** — previous three sessions were all blocked by the login gate with no test credentials. The user gave me a test account this session (added to `CLAUDE.md` → `docs/TEST_CREDENTIALS.local.md`, gitignored since this is a public repo — see the entry below). Used it for real: cleared `localStorage` to simulate a brand-new device, logged into the real test account against the local dev server (same Supabase backend as prod), confirmed via direct `localStorage` inspection that the real cloud data (9999 coins, 8 cosmetics, 12 room items) loaded correctly instead of getting wiped, and confirmed the exact new `resolveSync` branch fired via console log (`"local never saved (lastSavedAt 0), remote has a real save"`). Reloaded again on the same device afterward and confirmed no regression (data stayed intact via the normal `"local wins"` timestamp path). Signed out and cleared localStorage afterward to leave the browser tidy.
- Not finished: nothing in the spec's 4 fixes (adjusted for the timing-gap correction).
- Blockers/risks found: none remaining for this bug. One accepted, documented tradeoff from the literal Fix 2 spec: if a guest (not logged in) closes the app within the brief async initial-load window, that session's daily-login coin bonus could be lost (not flushed before close) — re-derivable next day, not a real-progress loss, judged acceptable.
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); optional Room polish (desk/chair iso faces, flagged 3 sessions ago).
- Needs Chatbot decision first: nothing blocking.

**2026-07-03 — Test account added for live browser testing:**
- Context: the user asked me to add a test account (URL/email/password) directly into `CLAUDE.md` so future sessions don't need to ask for credentials — this closes a gap I'd flagged in each of the prior three sessions (Room decoration, battle enemy art, icon-first pass, Room redesign — all blocked from live-testing by the login gate).
- **Checked repo visibility before complying and found `bossberry/kidquest` is a PUBLIC GitHub repo** — committing the literal password as asked would have made it permanently visible in git history the moment it was pushed. Flagged this to the user via AskUserQuestion instead of silently complying or silently refusing; they picked the gitignored-file option.
- Built: actual credentials live in `docs/TEST_CREDENTIALS.local.md` (gitignored, confirmed untracked via `git check-ignore`). `CLAUDE.md` gained a "Live Browser Testing" section pointing to that file, plus the URL and the UUID-safety note (not secret, safe to commit) reminding agents to only test destructive DB ops against the test account's own UUID, never Chopin's real UUID.
- Verified: `git show --stat` on the commit confirms only `.gitignore`/`CLAUDE.md` changed, no credential in the diff.
- Ready to start next: this immediately paid off in the very next task this session (the cross-device sync fix) — first real live end-to-end verification in 4 sessions.
- Needs Chatbot decision first: nothing blocking.

**2026-07-03 — Room UX redesign: full-screen canvas, tap-anywhere, unified bottom sheet:**
- Context: screenshot review of the isometric room (from the two prior Room sessions) found the room canvas small and floating in empty dark space, plus a UX friction point — the child had to tap a zone tab (floor/left wall/right wall) before they could tap a slot. This session fixes both, replacing "select zone → select slot" with "tap anywhere, zone auto-detected," and adds decorating feedback (sparkle + bounce) and ambient visual polish (vignette, wallpaper texture, ceiling shadow, ambient light).
- Built: `roomScene.js` — `hitTestZone`'s `zone` param made optional (omit → tests floor→left_wall→right_wall, returns first hit); new `drawTapHints()` (all-zone pulsing "+" on empty slots / glow on occupied, gated behind a `showTapHints` param only `Room.jsx` sets); `drawFloorFurniture`/`drawWallFurniture` gained optional bounce-scale params; added a wall-item hanging shadow, floor vignette, wall wallpaper texture + ceiling-shadow strip, and ambient light glow (all `!small`-only). `Room.jsx` near-fully rewritten: canvas is `flex:1` (ResizeObserver-driven, no more fixed 380px height), header shrunk to a top-left pill overlay, zone-tab switcher deleted, old picker+action modals merged into one unified bottom sheet showing the full zone catalog (owned/elsewhere/locked-with-price states, not just owned items like before), one self-stopping rAF loop drives hint fade + placement bounce + a sparkle burst reusing `particles.js`'s existing `mkSparks`/`tickEffects` (the same system battle effects use — no new particle code).
- Process: user explicitly requested Opus for this task, same as the icon-first pass. Spawned a fresh Opus subagent with the full spec plus grounding detail I'd already worked out from reading `Room.jsx`/`roomScene.js`/`roomItems.js` myself first (exact function signatures, the existing `hitTestZone`/`slotCenter`/`drawRoomScene` API, the `particles.js` reuse opportunity). **The agent hit the same background session-quota wall documented in the Pandora world-map work** — it landed only the `hitTestZone` change before being cut off mid-task. I confirmed via `date` that the quota's reset estimate had passed, then resumed the SAME agent via `SendMessage` to its agent-id (not a fresh restart) so it kept full context of what was/wasn't done — it picked up exactly where it left off and completed the remaining 4 fixes.
- Verified: reviewed the full diff of both files line-by-line myself (not just the agent's self-report — this has caught real issues in the last two sessions, e.g. the stale comment and the tofu-box glyphs, so I keep doing it). Confirmed via `grep` that `DecoratedRoom.jsx:93` and `RoomScene.jsx:63` (RoomVisit + FriendsScreen thumbnail) pass none of the new params — unaffected except for the non-gated ambient polish, which is intended. `npm run build` clean. **Tried a new live-verification approach**: mounting `<StateProvider><Room/></StateProvider>` standalone via dynamic `import()`, which bypasses the Supabase login gate entirely since `Room.jsx` only needs app state, not auth (this could be a reusable technique for future sessions blocked by the login gate!) — but hit a Vite dev-server HMR artifact instead: my direct `import('/src/context/StateContext.jsx')` and `Room.jsx`'s own internal import of the same file resolved to two different HMR-versioned module instances (different `?t=...` timestamps in the stack trace), so `useAppState` threw "must be used inside StateProvider" even though one genuinely was mounted above it. Cleaned up the injected test DOM and restored the browser's real `localStorage['kq_state']` (there was pre-existing real-looking test data in it from earlier sessions — captured and restored it, didn't touch/lose it). Fell back to code-review-only verification, same limitation as the last two sessions.
- Not finished: nothing in the spec's 5 fixes. Deliberately skipped per the spec's own allowance: real swipe-to-dismiss on the bottom sheet (tap-outside-scrim only) and a dedicated swap button (redundant now that tapping any other owned item while a slot is occupied already swaps it).
- Blockers/risks found: **this is the fourth session in a row** blocked from fully live-testing UI work by the login-gate/no-test-credentials gap (Room decoration, battle enemy art, icon-first pass, now this). The standalone-`StateProvider`-mount idea is promising for *state-only* screens (no auth needed) but needs the HMR module-identity issue solved first — worth exploring a `?import` query-free approach or testing right after a fresh page load with no prior HMR churn in the session, if this comes up again.
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); optional Room polish (desk/chair iso side/top faces, flagged 2 sessions ago); resolving the recurring test-credentials/guest-mode gap.
- Needs Chatbot decision first: nothing blocking. Same open item as last session — the guest-mode/test-credentials gap.

**2026-07-03 — Icon-first UI pass (all text-only buttons given icons):**
- Context: a report-only audit earlier the same day catalogued every button/tab/action in the app and found ~41 elements showing text with no icon at all — a real problem since the primary user can't reliably read yet. This session fixed all of them.
- Built: `BottomNav.jsx` (5 tabs → real icons 🏠🛒🏡📊👥, active tab glows gold + scales), `Collection.jsx` (wearable sub-tabs 🎩/😎, equip toggle now icon-only ✅/❌ with a glow when equipped, furniture place button gets 🏠), `Room.jsx` (zone tabs 🟫/◧/◨, remove/swap now icon-only 🗑️/🔄), `MoveSelectBattleMode.jsx` (TTS 🔊 enlarged+icon-only, item use/cancel KEEP a tiny label alongside their icon — deliberate judgment call, full icon-only risked use-vs-cancel confusion mid-battle — victory-return gets 🏃💨, teach-intro dismiss gets an ⚔️ prefix but keeps its full guiding text since it's a new player's first-ever battle instruction), `WorldScreen.jsx` (NPC/sign buttons now icon-only, boss-flee/maze dialogs get icon+tiny-label, item-bag "CLOSE" → ✖️), `FriendsScreen.jsx` (view-stats → 📊), `Home.jsx` (hatch CTA shrunk to 👆+"แตะ!"), `TreasureSlot.jsx` (collect → 🎁). Every touched button got a 44×44px min touch target + `aria-label` where it went icon-only.
- Process: implemented by a fresh Opus subagent (user explicitly requested Opus for this task) working from a detailed per-file spec built from the earlier audit. I (coordinator, Sonnet) reviewed every file's diff individually before accepting — all 8 were minimal, scoped, and matched the spec. Found and fixed one issue during review: a stale code comment above the WorldScreen NPC button claimed "kept the text label... so the action stays legible for a young child" — that comment predated this pass and was left contradicting the new icon-only code; corrected it.
- Verified: `npm run build` clean. Only `BottomNav.jsx` was confirmed live — it's a stateless component (no context hooks), so I mounted it standalone via a dynamic-import test harness in Chrome (same technique used in recent sessions) and screenshot-confirmed icons render crisp, active-tab glow works, labels are legible. The other 7 files all depend on app state context (`useAppState`/`useCompanion`/Supabase session) and the app is fully gated behind login with no test credentials available this session (same recurring limitation flagged in the two prior sessions) — those were verified by code review only, not live rendering.
- Not finished: nothing in the spec's 8 sections. Deliberately out of scope (per the spec): the legacy unreachable practice screens (GameThai/GameMath/GamePhonics/GameMathBattle/GameShop/LevelSelector).
- Blockers/risks found: the recurring "no test credentials / no guest-mode entry point" issue (flagged in the last session too) is now blocking live verification of THREE sessions' worth of UI changes in a row (Room decoration, battle enemy art, and now this). Worth Boss either sharing a standing test account, or deciding whether the guest-mode gap itself should get fixed.
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); optional Room polish (desk/chair iso faces, flagged two sessions ago); resolving the test-credentials/guest-mode gap so future sessions can actually drive the live UI.
- Needs Chatbot decision first: nothing blocking. The implementing agent flagged the Room.jsx wall-zone icons (`◧`/`◨` unicode box-glyphs) as unverified on a real device — I checked directly in Chrome and they render as blank tofu boxes (unsupported glyph), so I swapped them to `⬅️`/`➡️` (confirmed rendering correctly) before this session ended; no longer an open question. Still worth a decision on the recurring guest-mode/test-credentials gap noted above.

**2026-07-03 — Battle screen enemies given the Pandora art style:**
- Built: `EnemyCanvas.jsx` (battle-screen enemy sprite) switched from the original flat pixel-art `drawEnemy()`/`drawEnemyHurt()` to the rounded/volumetric `drawEnemyPandora()` that was built for the world map back in Stage 4 of the Pandora rewrite — enemies now look the same in battle as they do walking around the map. Wrapped in a fixed 70×70 virtual coordinate space (ground-anchored at `cx=35, groundY=41`, same convention `drawPandoraTree`/`renderPlayerPandora` use), scaled to whatever `size` the canvas is given (120 and 80, the two real call sites). Sized the virtual space by checking every one of the 10 types' vertical/horizontal extent so nothing clips against the canvas edge — bunny ears (tallest, ~36 units above ground) and ghost wisp's ambient halo (widest, a 24-unit-radius circle centered ON the ground point, extending both above and below it) were the two tightest fits and both clear with margin.
- `frame` passed as a static `0` — slime wobble / ghost wisp's vertical bob don't animate in battle (matches the old flat art's behavior, which was also static; a battle-screen animation loop was out of scope for a pure art-style swap). `drawEnemyHurt`'s eye-swap-on-hit has no Pandora equivalent and was dropped — hit feedback is unchanged (`hitFlash` CSS filter + `enemyDefeating` fall/fade). The original flat sprite set now has zero remaining callers anywhere in the app; left in `drawEnemy.js` as dead code rather than deleted (deletion wasn't asked for).
- Verified: `npm run build` clean. Could **not** exercise a real battle live — the app is fully gated behind a Supabase login screen (only email/password fields, no visible guest-mode entry point despite `CLAUDE.md`'s "guest mode: full app must work without login" rule — flagging this as a possible existing gap, not something I touched) and I have no test credentials this session. Verified instead via a dynamic-import test harness injected into the running dev server's page (same technique the Room decoration session used): rendered all 10 enemy types at both real battle canvas sizes (120px, 80px) directly via `import('/src/lib/drawEnemy.js')`, screenshot-confirmed every type renders distinctly and fully on-canvas with zero clipping (zoomed in specifically on the two tightest-fit types), zero console errors.
- Not finished: nothing in scope. Not attempted (out of scope for this task): finding/restoring a guest-mode or test-login path so future sessions can drive the actual live UI instead of dynamic-import workarounds.
- Blockers/risks found: none for this change. The pre-existing login-gate-with-no-guest-entry-point (noted above) affects live-testing ANY screen, not just this one — worth a decision on whether that's intentional (dev-only friction, fine) or a real regression against the `CLAUDE.md` guest-mode rule.
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); optional Room polish (desk/chair iso side/top faces) flagged in the prior session below.
- Needs Chatbot decision first: nothing blocking. Worth a decision at some point: is there supposed to be a guest/skip-login path in the current UI, or was that rule aspirational and never fully wired up?

**2026-07-02 — Isometric Room decoration system (finished the interrupted iso rewrite):**
- Built: wired the previously-orphaned `src/lib/roomScene.js` (iso geometry/renderer) into real data end-to-end. Added `allowedZones` + iso draw treatment (contact-shadow grounding via `withGround()`, iso side/top faces on boxy items) to all 12 `roomItems`. Added `migrateStateShape()` logic that resets stale numeric-key `roomLayout` to `{}` (bumping `lastSavedAt`) while never touching `ownedRoomItems`. Renamed `PLACE/REMOVE_ROOM_ITEM` payload `slotIndex`→`slotKey`. Rewrote `Room.jsx` as a single canvas that draws + hit-tests, with a 3-tab zone switcher (พื้น/ผนังซ้าย/ผนังขวา), zone-filtered picker, and the existing remove/swap/toast UX. Added zone badges to Collection furniture cards. Updated CURRENT_STATE / TASKS / CHANGELOG.
- Not finished: none of the task scope. The one thing NOT done vs the brief: I did **not** edit `CLAUDE.md` (the brief listed it, but my operating rules forbid changing the project rules/instruction file on an agent's say-so — flagging so a human can add a schema note there if desired).
- Blockers/risks found: (1) The live Room screen is behind a Supabase login gate (guest/session not present in this environment) — could not drive the real editor UI by hand. Verified functionally instead by dynamic-importing the dev-server source: full iso render (screenshot-confirmed), `hitTestZone(slotCenter())` round-trips for all 3 zones, and every `migrateStateShape` case (old resets + preserves owned, new/empty untouched). Build clean, zero console errors. (2) Confirmed anon-key REST reads of `eggs.state_json` return `[]` (RLS) — the suspected past `ownedRoomItems` wipe on a child's account cannot be inspected or restored from here; needs Boss/service-role. No DB writes attempted.
- Ready to start next: (optional polish) add iso side/top faces to the remaining floor items (desk, chair) for full visual consistency; add a walker/companion back into the iso editor if desired.
- Needs Chatbot decision first: whether wall zones should get more than 2 item types (only window + wall_art are wall-appropriate today) — if the child should be able to fill 8 wall slots per wall, we need more wall-mountable furniture designs.

**2026-07-02 — Pandora-style pseudo-3D world map Stage 6/6: polish + delete old renderers (rewrite complete):**
- Context: final stage of the Pandora-style world-map rewrite (Stage 5 entry directly below). The user was asked whether Stage 6 should be polish-only (keep flat/iso/pandora coexisting behind flags) or polish + delete the old renderers to make Pandora the real sole default — chose **polish + delete**. Still on the agent-quota wall from earlier this session, implemented directly by the coordinating session again.
- Polish built: grass top-highlight stripe gated to ~55% of tiles (was every tile, reading as a faint continuous grid-line — now broken/organic); owl NPC given a dedicated silhouette (pale face patches, orange beak, ear tufts) instead of a uniform brown blob that read as under-differentiated from an enemy; enemy "!" alert indicators ported in for woken sleepy_bunny / aggro'd snake / world boss (previously flat-renderer-only, now working in Pandora too); ghost_wisp's gentle vertical bob ported in.
- **Before deleting anything, audited real cross-file dependencies** (critical — the original flat `drawEnemy()`/`DRAW_FNS`/`drawEnemyHurt()` in `drawEnemy.js` turned out to still be the ONLY enemy art the battle screen's `EnemyCanvas.jsx` uses; deleting those would have broken every battle in the game, not just the world map). Confirmed via grep that `tileEngine.js`'s `renderMap`/`renderPlayer`/`getCamera`/flat draw functions and ALL isometric exports had zero external callers outside `tileEngine.js`/`useWorldGameLoop.js` themselves — safe to delete. `worldDrawHelpers.js`'s flat `drawChest`/`drawPlayerGlow` were also safe (only caller was the now-deleted flat branch) but `drawMazePortal` and its local `TILE` constant were NOT (still reused as-is for Pandora's portals) — kept both.
- **Discovered and fixed a real gap before deleting**: the flat renderer's branch in `useWorldGameLoop.js` was the ONLY place several features were wired up — maze entry/exit portal rendering, the maze fog-of-war/torch-ring DOM overlay position updates, the ambient player glow ring, and the saiyan/rainbow-star boost's rotating-hue player shadow. None of these had ever been ported to the Pandora branch across Stages 1-5 (out of scope for those stages, which were purely additive). Deleting the flat renderer without porting these first would have silently broken maze navigation/atmosphere and the saiyan boost's visual feedback. Ported all four into the Pandora branch: portals are now Y-sorted like every other object (correct player-in-front/behind occlusion), fog/torch position off the player's Pandora screen coordinates, glow reuses a new `drawPandoraPlayerGlow` (2x-scaled version of the original), saiyan effect wraps the player's Y-sort draw closure exactly like the original wrapped `renderPlayer`.
- Deleted: `tileEngine.js` — the entire flat renderer (`renderMap`, all flat `drawX` functions, the `C` palette, `renderPlayer`, `getCamera`) and the entire isometric renderer (`isoProject`/`isoUnproject`, `ISO_W`/`ISO_H`, all `drawIso*` functions, `renderMapIso`, `renderPlayerIso`) — kept `tileHash()` (Pandora still uses it) and `T`/`MAP_COLS`/`MAP_ROWS`/`canMove`/exit helpers (still needed). `worldDrawHelpers.js` — flat `drawChest`, flat `drawPlayerGlow`. `useWorldGameLoop.js` — both debug flags and all conditional branching (Pandora render path is now unconditional); the now-dead `renderEnemies`/`renderChests` local helper functions; the unused `eggColorRef` destructure. `WorldScreen.jsx` — a stale unused import (`drawChest`/`drawPlayerGlow` were imported but never actually called in that file, even before this rewrite).
- Verified live (Chrome, dev server, account "โชแปง"): fresh navigation to the world map with NO debug flags set loads Pandora correctly (confirms it's genuinely the sole default now) — player glow ring clearly visible, enemies/chests render, zero console errors. **Walked directly onto a live Egg Pawn enemy and played a real battle round** (attack move, HP dropped 198→173) to specifically confirm the battle screen's `EnemyCanvas` — which shares `drawEnemy.js` with the world map — still renders correctly post-deletion; it does, byte-for-byte untouched. `npm run build` clean, bundle size dropped back down (~773 kB, matching pre-iso-experiment size) confirming the dead code is genuinely gone, not just unreferenced.
- Not independently re-verified live this session: the maze fog/torch/portal port (ported by careful code reasoning from the original working flat-renderer logic — matched every coordinate/timing detail, but did not personally navigate into a maze to screenshot-confirm) and the saiyan-boost rainbow glow (no active boost item available in the test account to trigger it). Both are straightforward, low-risk ports of logic that was already correct; flagged here in case anyone notices a visual issue in either during real play.
- Docs: Updated `docs/CURRENT_STATE.md` (Map System section rewritten to describe the Pandora renderer; player-sprite line updated), `docs/TASKS.md` (new Done entry summarizing all 6 stages), `docs/CHANGELOG.md` (full dated entry). `docs/CLAUDE.md` grepped for tile-engine/world-map references — none found, nothing to update there.
- Blockers/risks found: None remaining — this closes out the whole 6-stage rewrite. The two things noted as "not independently re-verified" above are the only loose threads.
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); the still-separately-pending isometric ROOM system rewrite (unrelated subsystem, `roomScene.js`/`Room.jsx`/`roomItems.js`/`Collection.jsx`/`state.js` — only `roomScene.js` partially done, still blocked on the same agent-quota wall as of this entry).
- Needs Chatbot decision first: Nothing blocking. Optional spot-check: someone should navigate into an actual maze screen and trigger a saiyan boost during real play to close the two "not independently re-verified" items above.

**2026-07-02 — Pandora-style pseudo-3D world map Stage 5/6: chests, signs, NPCs:**
- Context: continuation of the Pandora-style world-map rewrite (Stage 4 entry directly below). Still on the agent-quota wall, implemented directly by the coordinating session again per standing direction.
- Built: `src/lib/worldDrawHelpers.js` — new `drawPandoraChest(ctx, cx, groundY, frame)` (box body + darker lid + lighter top-face strip + pulsing gold clasp + drop shadow), fully separate from `drawChest()` above it (untouched). `src/lib/tileEngine.js` — new `drawPandoraSignStanding()` (post + beige board + squiggly "text" lines) and `drawPandoraNPCStanding()` (round head + body + a waving-arm stroke, using a small local `pandoraFriendlyBody()` shading helper since tileEngine.js doesn't import from drawEnemy.js), both wired into `renderMapPandora()`'s existing per-tile loop the same way Stage 2's trees/rocks are — `T.SIGN`/`T.NPC` tiles push a standing entry (ground tiles under them already correctly defaulted to grass via the existing `switch`'s `default` case, no change needed there). `src/hooks/useWorldGameLoop.js` — the pandora branch now also iterates `chestsRef.current`, skips `opened` ones (matching the flat renderer's `renderChests()` convention), and pushes each into the same Y-sort `extraEntities` list as the player/enemies via the identical ground-anchor formula.
- Verified live (Chrome, dev server, account "โชแปง", `window.__kq_pandoraDebug=true` + `__kq_isoDebug=false`): screenshot + zoomed crop confirm two chests render correctly (box shape, lid contrast, gold clasp, shadow) on the "ป่าเหนือ" screen. The owl NPC (`BM_MAP`'s `NPC('owl')` at col 6/row 3) renders without crashing but reads as a fairly plain brown blob — noting as a Stage 6 polish candidate (a beak or more owl-specific silhouette would help distinguish it from an enemy at a glance; it currently doesn't look obviously wrong, just under-differentiated). Could not get a specific screenshot of the sign this session (D-pad automation didn't reliably cover the distance to it before time/effort tradeoff stopped being worth it) — it goes through the exact same `standing.push({y, draw})` mechanism already proven correct for 4 other standing-object types (trees/rocks/NPCs/chests), so I'm confident it renders correctly by construction, just wasn't personally screenshot-verified. Confirmed via the flat-renderer fallback check that both a sign and the owl NPC do exist and render on this map (so if picked up again, they're on-screen near the path, easy to reach). Zero console errors. `npm run build` clean.
- Not finished: Chest-open interaction wasn't re-exercised end-to-end this stage (same reason — automation didn't reach a chest's exact tile in the time available) — but this is pure shared game-state logic (`tryMove`'s `hitChest` check + `chestsRef.current` mutation), completely unchanged from the flat renderer, and was implicitly working throughout every earlier stage's testing since the same collision code runs regardless of which renderer is active.
- Blockers/risks found: Same quota situation as Stages 1-4. Only Stage 6 (polish/cleanup) remains after this — worth reserving for a fresh subagent if the quota resets, since it involves the "replace the default renderer" decision flagged back in Stage 1 plus auditing all 5 stages' minor noted rough edges (grass grid-line hint, missing enemy "!" alerts/ghost bob, owl NPC distinctiveness) rather than just adding new code.
- Ready to start next: Stage 6/6 — polish (texture variation, lighting consistency, z-sorting artifact check) + the final "make this the real default" cleanup decision.
- Needs Chatbot decision first: For Stage 6 — should the flag-gated coexistence (flat/iso/pandora all present, pandora just defaulting on) be the final state, or should Stage 6 actually delete the original flat renderer + the now-fully-superseded iso renderer once Pandora is confirmed solid? This wasn't explicitly specified and materially changes Stage 6's scope (pure polish vs. polish + deletion/cleanup).

**2026-07-02 — Pandora-style pseudo-3D world map Stage 4/6: all enemies redrawn, Y-sorted:**
- Context: continuation of the Pandora-style world-map rewrite (Stage 3 entry directly below). Still on the agent-quota wall, implemented directly by the coordinating session again per standing direction.
- Built (`src/lib/drawEnemy.js`: 10 new functions — `pSleepyBunny`/`pBouncySlime`/`pFoxKit`/`pGhostWisp`/`pSnake`/`pEggPawn`/`pLeafSprite`/`pGrumpyMole`/`pMushroomImp`/`pBabyZombie`, a shared `pBody()` volumetric-shading helper and `pShadow()` drop-shadow helper, a `PANDORA_DRAW_FNS` lookup, and the exported `drawEnemyPandora(ctx, type, cx, groundY, frame)` entry point — all completely separate from the original `DRAW_FNS`/`drawEnemy()`/`drawEnemyHurt()` above them in the same file, which remain byte-for-byte untouched; `src/hooks/useWorldGameLoop.js`: the pandora branch now iterates `enemiesRef.current` each frame, skips `defeated`/`dead` ones — matching the flat renderer's `renderEnemies()` convention — and pushes each live enemy into the same `extraEntities` Y-sort list the player uses, keyed by the same ground-anchor formula (`row*TILE+TILE*0.8` minus camera) so enemies, player, and trees/rocks all sort into one consistent depth order). Each enemy type keeps a distinct silhouette/color identity from the original (bunny=white+pink ears, slime=translucent green+wobble, fox=orange+tail, ghost=purple+wavy+glow, snake=green S-curve segments, egg_pawn=red+white belly+antennae, leaf_sprite=green blob+leaf nubs, grumpy_mole=brown+glasses, mushroom_imp=red cap+white spots, baby_zombie=grey-green+red X eyes) but rendered as rounded/volumetric ellipse compositions with the shared upper-left-light/lower-right-shade convention instead of flat pixel-art rects.
- Verified live (Chrome, dev server, account "โชแปง", `window.__kq_pandoraDebug=true` + `__kq_isoDebug=false`): screenshot + zoomed crop show egg_pawn, fox_kit, and baby_zombie all rendering distinctly and correctly with visible drop shadows on the "ป่าเหนือ" starting screen. Zero console errors. Attempted to force a direct-collision battle trigger for full-loop verification but only got the player adjacent to (not exactly on) a wandering enemy's tile before it moved off — not a bug, just imprecise D-pad timing against autonomous enemy movement; battle-trigger/collision logic itself is untouched shared game state, already directly confirmed working with the pandora renderer active back in Stage 3 (walking into a `baby_zombie` correctly opened the battle-select screen) and again incidentally during this stage's testing on the flat-renderer fallback check. Re-confirmed both debug flags OFF renders the untouched original flat renderer. `npm run build` clean.
- Not finished: No "!" alert indicators above woken/aggro'd enemies or the world-boss marker (the flat renderer's `renderEnemies()` draws these) — deferred as a Stage 6 polish item, not core to "enemies redrawn" scope. Ghost wisp's gentle vertical bob (present in the flat renderer) was not ported either — same deferral.
- Blockers/risks found: Same quota situation as Stages 1-3. Stage 5 (chests/signs/NPCs — the last set of static+interactive objects) should be a comparable-or-smaller chunk than this stage. Stage 6 (polish/cleanup, including the real "replace the default" decision flagged back in Stage 1) is the one most worth reserving for a fresh subagent's fuller attention if the quota resets before then, since it involves auditing/removing dead code paths, not just adding new ones.
- Ready to start next: Stage 5/6 — chests, signs, NPCs drawn in the new style, all their existing interaction triggers (open/read/talk) confirmed still working.
- Needs Chatbot decision first: Nothing blocking Stage 5.

**2026-07-02 — Pandora-style pseudo-3D world map Stage 3/6: player egg with shadow, movement tween, real camera-follow:**
- Context: continuation of the Pandora-style world-map rewrite (Stage 2 entry directly below). Still on the agent-quota wall (checked time again before proceeding — not reset yet), implemented directly by the coordinating session per the user's standing direction to continue in the main session rather than wait.
- Built (`src/lib/tileEngine.js`: new `renderPlayerPandora()`, `renderMapPandora()` signature extended with an optional `extraEntities` param; `src/hooks/useWorldGameLoop.js`: the pandora branch rewritten): `renderPlayerPandora(ctx, groundX, groundY)` draws a soft drop-shadow ellipse then the companion egg sprite scaled to 40×48 (up from the original 32×32), anchored so its feet sit at the ground point — same anchor convention as Stage 2's trees/rocks. `renderMapPandora()`'s Y-sort `standing` list is now seeded with `extraEntities` (an array the caller supplies) before trees/rocks get pushed onto it, so the player sorts into the SAME list/pass as trees rather than being a separate always-on-top or always-behind layer — this was designed in Stage 2 already (see that entry) specifically so this stage wouldn't need to retrofit the sort mechanism. `useWorldGameLoop.js`'s pandora branch now computes a real player-follow camera (previously static in Stages 1-2, since there was nothing to follow) using the brief's own formula (`playerDisplayX/Y * TILE + TILE/2 - viewW/H/2`), clamped to map bounds the same way the original `getCamera()` clamps for the flat renderer, plus the same HUD/MissionPanel vertical offset the flat renderer already applies (added for UX parity, not explicitly requested by the brief — noted as a deviation below). The player's screen-space ground position (`worldX*TILE+TILE/2`, `worldY*TILE+TILE*0.8` minus camera, per the brief's exact anchor formula) is packaged as one `extraEntities` entry and passed into `renderMapPandora()`.
- Verified live (Chrome, dev server, account "โชแปง", `window.__kq_pandoraDebug=true` + `__kq_isoDebug=false`): player egg renders with a visible shadow at the new larger scale (screenshot + zoomed crop confirm it reads as standing on the path), D-pad movement confirmed working (tween + tile-space position changes visible across screenshots), zero console errors. Re-confirmed both debug flags OFF still renders the untouched original flat renderer (now also showing enemies moving normally, confirming shared game-state is unaffected by which renderer is active — only the drawing differs). `npm run build` clean.
- Not fully verified: the brief's Stage 2 acceptance line ("can walk behind trees") — this map (`BM_MAP`, the "ป่าเหนือ"/starting-path screen) only places trees on the impassable border ring, and the row immediately above a border tree is out-of-bounds, so there's no walkable tile from which the player could visually pass "behind" a border tree's canopy on THIS specific screen. The underlying Y-sort mechanism is the same one already proven correct for tree-vs-tree occlusion in Stage 2, and the player now uses the identical camera-adjusted screen-Y coordinate space, so I'm confident it's correct by construction — but I want to flag that I could not produce a screenshot proving it on this map. The boss-tier maps do place interior tree-wall segments (`generateBossMap()`, `tileMaps.js`) where this WOULD be directly testable; worth a follow-up check once one is reachable, or in Stage 4+ if enemies end up drawn near those walls anyway.
- Deviation from the brief: added the HUD/MissionPanel vertical camera offset (not in the brief's camera formula) purely for UX parity with the working flat renderer, so the player isn't visually crowded under the HUD bar. Low-risk, easy to revert if it looks wrong once compared side-by-side with a screenshot at Stage 6 polish time.
- Blockers/risks found: Same quota situation as Stages 1-2. Stage 4 (all 9 enemy redraws) is a meaningfully bigger chunk of work than Stages 1-3 combined — flagging that as the point where a proper subagent's isolation would pay off most, if the quota resets before then.
- Ready to start next: Stage 4/6 — enemy redraws (rounded/volumetric, drop shadows, upper-left lighting per-type) + Y-sorted into the same `extraEntities` list as the player.
- Needs Chatbot decision first: Nothing blocking Stage 4.

**2026-07-02 — Pandora-style pseudo-3D world map Stage 2/6: trees + rocks/walls, Y-sort depth system:**
- Context: continuation of the Pandora-style world-map rewrite (Stage 1 entry directly below). Still blocked on the agent-quota wall from earlier this session (~22h estimate, not yet reset — checked current time before proceeding), so this stage was again implemented directly by the coordinating session at the user's explicit direction ("can do it with main not agent"), not via a fresh subagent.
- Built (`src/lib/tileEngine.js` only): `drawPandoraShadow()` (shared soft elliptical drop-shadow helper), `drawPandoraTree()` (trunk box + two-tone canopy circle + upper-left highlight blob, anchored at the tile's ground-contact point with the canopy intentionally extending above the tile's top boundary for real height), `drawPandoraRock()` (rounded boulder via overlapping ellipses — darker-left/lighter-right faces + thin top highlight arc; per the brief, walls reuse the rock visual since there's no separate "wall" concept in the art style). `renderMapPandora()` restructured into two conceptual passes: ground tiles still draw in one flat sweep first (trees/walls draw plain grass underneath, same as any other tile), then trees/rocks are collected into a `standing` list keyed by their ground-contact screen-Y and drawn AFTER the full ground sweep, sorted ascending by Y — this is genuine Y-sort (not row-order painter's algorithm like the iso track's Stage 2), so it'll correctly extend to sort against entities (player/enemies) once those exist in Stage 3/4, not just among trees themselves.
- Verified live (Chrome, dev server, account "โชแปง", `window.__kq_pandoraDebug=true` + `__kq_isoDebug=false`): the border-tree ring (from `tileMaps.js`'s `BM_MAP`) renders as a continuous, gap-free wall of canopies with visible trunks and shadows — screenshot + zoomed crop confirm clean overlap with no z-fighting. Zero console errors. Re-confirmed both debug flags OFF still renders the completely untouched original flat renderer. `npm run build` clean.
- Not finished: No player or enemies yet (Stage 3/4), so the Y-sort system's "walk behind a tree" behavior described in the brief's Stage 2 acceptance line can't be visually demonstrated yet — that requires the player sprite, which is explicitly Stage 3 scope per the brief's own stage breakdown. The `standing` list's sort-by-Y mechanism is built to already support this once the player is added (just needs to also push a `{y: playerGroundY, draw: ...}` entry into the same list), not a separate system that'll need retrofitting.
- Blockers/risks found: Same as Stage 1 — agent-quota wall still in effect, Stages 3-6 involve more code (player+shadow, all 9 enemy redraws, chest/sign/NPC art, final polish/cleanup) and would benefit from a proper subagent's isolation once available, though the user has directed continuing in the main session for now.
- Ready to start next: Stage 3/6 — player egg with shadow, movement tween, Y-sorted against trees (the acceptance criterion from Stage 2's brief text becomes testable here).
- Needs Chatbot decision first: Nothing blocking Stage 3.

**2026-07-02 — Pandora-style pseudo-3D world map Stage 1/6: ground tiles only:**
- Context: a design pivot away from the isometric world-map track (Stages 1-3 directly below, already merged) toward a different, non-isometric technique modeled on the mobile game Pandora — Y-sorted top-down depth (objects sort by screen-Y ground-contact point) instead of diamond-tile iso projection, TILE upgraded 16→32px, richer painted-style textures. Full spec saved at `docs/HANDOFF_pandora_worldmap.md`. This session (2026-07-02) hit a hard agent-quota session limit partway through the iso track's Stage 3, and again immediately when a fresh agent was launched for this Pandora Stage 1 — both agent runs were cut off with near-zero progress. Rather than wait ~22h for the quota to reset, the coordinating session (Claude Code, no subagent) implemented Stage 1 directly, since it's small and well-scoped.
- Built (`src/lib/tileEngine.js`: `PANDORA_TILE=32` constant, a `P` color palette, `drawPandoraGrass`/`drawPandoraPath`/`drawPandoraTallGrass`/`drawPandoraWater`, and `renderMapPandora()`; `src/hooks/useWorldGameLoop.js`: one new `window.__kq_pandoraDebug` branch, checked BEFORE the existing `window.__kq_isoDebug` branch so the two experiments can never collide, each fully independent): ground tiles only, no trees/objects/entities/Y-sort yet (that's Stage 2+). Grass gets 3-5 stable per-tile speckle dots (hashed from col/row via the existing `tileHash()` helper — reused from the iso track's Stage 2, not duplicated) + a faint top-edge highlight stripe. Path gets a lighter center patch + 4-6 pebble dots. Tall grass = grass base + 6-8 leaning blade strokes in two greens. Water = the existing 2-frame flicker convention, drawn as shimmer lines instead of a flat color swap. Camera is still static/map-centered (no player drawn yet this stage, matching the iso track's identical Stage 1 approach — camera-follow only makes sense once there's something to follow).
- Deviation from the brief: the brief frames this as an outright "replace the flat GB-style tile engine," but per the same pattern that worked well for the iso track, Stage 1 was built behind its own debug flag alongside (not overwriting) the original flat renderer and the dormant iso renderer, so all three can be compared/reverted independently while this is built out in stages. A true replacement (deleting the old code, changing the default) is a final-step decision, not a Stage-1 one — flagging so whoever does Stage 6 doesn't skip that cleanup.
- Verified live (Chrome, dev server, account "โชแปง"): `window.__kq_pandoraDebug=true` (with `__kq_isoDebug=false`) shows readable, textured grass/path/tall-grass — screenshot + zoomed crop confirm speckle dots, path pebbles, and blade clusters render correctly and don't flicker frame-to-frame. Noted one minor cosmetic artifact worth a Stage 6 look: the grass top-highlight stripe is faintly visible as a repeating horizontal line across the full-grass background — reads slightly grid-like at a distance, not wrong, just not fully "painted." Re-confirmed the flag OFF still renders the completely untouched original flat top-down renderer (tile grid, enemies, player, camera-follow) exactly as before. Zero console errors either way. `npm run build` clean.
- Not finished: Stages 2-6 (trees/rocks/walls + Y-sort; player egg + shadow; enemies upgraded; chests/signs/NPCs; final polish + the real replace-the-default cleanup).
- Blockers/risks found: The agent-quota session limit is still in effect as of this entry (~22h from when it hit, roughly 2026-07-03 12:00 Asia/Bangkok) — Stage 2+ should go back through a proper subagent once that resets (per the established staged-review pattern: fresh Opus agent, one stage at a time, coordinator verifies independently before authorizing the next stage), rather than continuing to hand-roll every stage in the main session, which doesn't scale well to the richer Stage 2-5 work (trees/Y-sort/enemy redraws are a lot more code than Stage 1's ground-only pass).
- Ready to start next: Stage 2/6 — trees/rocks/walls drawn, Y-sort depth system introduced (player can walk "behind" a tree). Full spec for all remaining stages is in `docs/HANDOFF_pandora_worldmap.md`.
- Needs Chatbot decision first: Nothing blocking Stage 2. Optional: confirm the Stage-1-behind-a-flag deviation (vs. an immediate hard replace) is the right call for the remaining stages too.

**2026-07-02 — Iso world map Stage 3/6: player egg in iso space with move tween:**
- Context: continuation of the 6-stage iso world-map rewrite (Stage 2 entry directly below). Stage 3 scope: draw the player's companion egg at the correct iso position with the existing walk tween, still no enemies/chests/camera-follow (Stage 4/5).
- Built (`src/lib/tileEngine.js`: new `renderPlayerIso()`; `src/hooks/useWorldGameLoop.js`: one call added inside the existing `window.__kq_isoDebug` branch): `renderPlayerIso(ctx, playerX, playerY, direction, walkFrame, eggColor, camX, camY)` reprojects the SAME interpolated tile-space `g.displayX/g.displayY` (the walk-tween math in `useWorldGameLoop.js` is completely unchanged — still tile-space floats) through `isoProject()` every frame, so the existing smooth glide now traces across the diamond floor instead of snapping tile-to-tile. Anchors the sprite at the tile's visual ground point (`px+ISO_W/2, py+ISO_H/2` — the same center-depth anchor Stage 2 used for tree trunks/sign posts), with the 28×28 sprite extending upward from there so the egg reads as standing ON the surface. Camera is still the STATIC map-centered camera from Stages 1-2 (camera-follow is bundled into Stage 5 alongside D-pad/collision remapping per the original spec) — the player visibly walks toward/away from a fixed view this stage, which is expected and not a bug.
- This session picked up this Stage 3 implementation mid-flight — an earlier agent run wrote the code correctly but got cut off by a session-usage limit before verifying/committing/documenting. I (the coordinator) reviewed the diff, verified it live myself (Chrome, dev server, D-pad movement — egg glides smoothly across the diamond floor, stands correctly on the surface, zero console errors; also incidentally confirmed downstream gameplay logic is untouched — walking into a `baby_zombie` enemy still correctly triggered the normal battle-select screen), and am completing the commit/push/docs steps directly rather than waiting for agent quota to reset.
- Not finished: Enemies/chests/portals still not drawn in iso view (Stage 4) — they still exist and can still trigger battles/pickups in tile-space logic even though nothing renders for them yet in iso mode, that's an expected rendering gap until Stage 4. No camera-follow yet (Stage 5, expected).
- Blockers/risks found: None for this stage. Flagging for whoever picks up Stage 4/5/6: a separate, much larger isometric ROOM system rewrite (unrelated files — `roomScene.js`/`DecoratedRoom.jsx`/`Room.jsx`/`roomItems.js`/`state.js`) was also in flight concurrently and got cut off by the same session-limit event, MUCH less complete (only `roomScene.js` touched so far, uncommitted) — do not confuse the two efforts or touch its files from the world-map track.
- Ready to start next: Stage 4/6 — enemies iso-rendered, movement/AI intact (per the original spec).
- Needs Chatbot decision first: Nothing blocking Stage 4.

**2026-07-02 — Iso world map Stage 2/6: tile types drawn iso (trees/water/tall-grass/path/flowers/signs):**
- Context: continuation of the 6-stage iso world-map rewrite (see Stage 1 entry directly below). Stage 2 scope: replace Stage 1's flat all-grass diamond with per-tile-type iso shapes, still no entities/player/camera-follow (that's Stage 3+).
- Built (`src/lib/tileEngine.js` only, plus one call-site line in `useWorldGameLoop.js`): `renderMapIso()` is now a two-pass renderer — pass 1 draws every tile's flat floor diamond (`drawIsoFloor()`, branching on tile type: grass plain; path = `C.PATH` + 2 deterministic dirt-speckle dots hashed from `(col,row)` so they don't flicker frame-to-frame; tall-grass = `C.TALL` base; tree = `C.TREE_BG` base; water = the old 2-frame `C.WATER_A`/`C.WATER_B` flicker, `frame` now threaded into `renderMapIso(ctx, tileMap, camX, camY, frame)` and from there into `useWorldGameLoop.js`'s call site as `g.frame`; flower = grass + the old `drawFlower` petal pattern flattened onto the diamond's surface, not raised; sign = grass base only). Pass 2 (`drawIsoRaised()`, run only AFTER pass 1 finishes for the whole map) draws the "raised" elements that stick up above the diamond, anchored at each tile's visual "ground" point (`px+ISO_W/2, py+ISO_H/2` — the diamond's center-depth, not its top vertex): tall-grass gets 2-3 short vertical blade strokes; trees get a small dark trunk box + a green canopy ellipse ~1.8× the diamond height tall (with a darker-underside shading ellipse); signs get a post + a small two-tone board box.
- Design note — why two passes, not interleaved: the original single-pass Stage-1 loop drew each tile fully (floor + any raised part) in strict row-major order before moving to the next tile. Interleaving would mean a tree's canopy (which visually spills upward into the screen-space of tiles at higher row/col — "closer to camera" in this projection) gets drawn, then a few iterations later a nearer tile's flat floor diamond gets drawn on top of it and clips the canopy where they overlap — a real z-fighting bug. Splitting into two full-map passes (all floors first, all raised elements after) guarantees no raised element is ever clipped by a floor tile, while still preserving correct occlusion BETWEEN raised elements themselves (row0→rowN-1 order within pass 2, so a nearer tree's canopy still correctly draws over a farther tree's canopy where they intersect).
- `T.WALL` — verified via `grep` that it's declared in the `T` constant map and referenced only in `canMove()`'s collision list; no map generator in `tileMaps.js` ever places it. No iso visual was added for it (would be inventing a visual for a tile type nothing in the game currently uses) — falls through to the grass default like Stage 1. Flagging in case a future stage/content pass wants to actually place walls/rocks somewhere.
- Also left as plain-grass fallback (unchanged from Stage 1, and intentionally out of this stage's listed scope): `T.EXIT_N/S/E/W` and `T.ITEM_SPOT` (no iso arrow/sparkle shape yet — not in the Stage 2 spec's tile list), and `T.NPC`/`T.ENEMY` (their tile-map cells correctly fall through to grass too, since the actual NPC/enemy/chest sprites aren't drawn until Stage 3/4 — chests aren't part of the tile map at all, they're separately spawned entities, so nothing was needed for them here).
- Not finished: Stages 3-6 remain (player sprite + camera-follow-with-clamping, enemies, chests/NPCs/portals, then flag/fallback removal once fully tested). Exit arrows and item-spot sparkles still render as flat grass in iso mode — may want an iso treatment in a later stage even though not explicitly scoped for Stage 2.
- Blockers/risks found: None. `eggAlgorithm.js`/`drawEnemy.js` untouched. Water's animated iso flicker could not be visually confirmed live in-app because `T.WATER` (`W`) is never actually placed by any current map generator (same `grep` check as the WALL note above) — implemented per spec anyway since Stage 2's task explicitly listed it, verified correct by code inspection (identical `frame < 30` branch reused from the original `drawWater`) and by the `frame` param now flowing end-to-end from `g.frame` through the call site.
- Live verification (Chrome, dev server on :5173, account "โชแปง", Home → "🗺️ ออกสำรวจ!", flag at its `true` default): screenshot + zoomed crops of the ป่าเหนือ (north forest) screen show a full ring of border trees with continuous, non-gapped canopy coverage (confirms the two-pass occlusion fix works — zoomed in on a tree-ring corner specifically, canopies blend seamlessly with visible individual trunks, no grass poking through gaps), two dark tall-grass patches with visible blade texture distinct from plain grass, a diagonal path strip with faint dirt-speckle dots, and several small teal flower-sparkle marks on the grass. Zero console errors. Toggled `window.__kq_isoDebug = false` live: original flat top-down renderer still renders correctly (grid tiles, player, wandering enemies, a chest) — fallback path unaffected. Flag set back to `true` before finishing. `npm run build` clean (172 modules).
- Ready to start next: Stage 3 — player sprite drawn in iso space + camera-follow (replacing the static map-centered camera) with map-bounds clamping.
- Needs Chatbot decision first: Nothing blocking for Stage 3 — direct continuation per the original 6-stage spec. Optional/non-blocking: should Stage 2 (or a later stage) also give `T.EXIT_*`/`T.ITEM_SPOT` proper iso shapes, or is flat-grass-with-arrow-later fine deferred to whichever stage handles portals?

**2026-07-02 — Iso world map Stage 1/6: empty iso floor renderer behind a debug flag:**
- Context: kicking off a 6-stage rewrite of the world-map renderer from the current flat top-down GB-palette canvas engine to an isometric (2:1) projection, one stage at a time so it can be checked incrementally. All gameplay logic (movement, battle triggers, chests, NPCs, enemies, maze, boss) stays completely intact through all 6 stages — this is a pure rendering swap. This session is Stage 1 ONLY: an empty iso floor, no entities, no interaction, no gameplay wiring.
- Built: `src/lib/tileEngine.js` — added `ISO_W`/`ISO_H` constants (32×16px diamond footprint), `isoProject(col, row, camX, camY)` (tile-space → iso screen pixels) and its inverse `isoUnproject()` (added now for symmetry, not used until a later stage's tap hit-testing), `drawIsoTile(ctx, px, py, fillColor)` (draws the 4-point diamond with a lighter-green top-left highlight stroke so tiles read as distinct), and `renderMapIso(ctx, tileMap, camX, camY)` (paints the full map back-to-front, row 0→MAP_ROWS-1, no viewport culling yet — every tile draws as a flat `C.GRASS` diamond regardless of its real type code; per-type iso shapes are Stage 2's job). `src/hooks/useWorldGameLoop.js` — wired a `window.__kq_isoDebug` runtime flag (module-level, defaults to `true`, only sets the default once so manual toggling during testing sticks): when true, `loop()` computes a static camera centered on the map (`isoProject(MAP_COLS/2, MAP_ROWS/2, 0, 0)` minus half the viewport, no clamping) and calls `renderMapIso()` only — no player/enemies/chests/portals drawn this stage, matching "empty floor" scope; when false/unset, falls through to the exact original flat-renderer path (`renderMap`/`renderEnemies`/`renderChests`/portals/glow/`renderPlayer`), unchanged.
- Not finished: Stages 2-6 remain — Stage 2 (per-tile-type iso shapes: trees/walls/water/tall-grass/signs/chests), then player sprite, camera-follow + clamping, enemies, and finally removing the flag/fallback once the iso path is fully built out and tested. `WorldScreen.jsx`, `tryMove`, the D-pad, collision, and all gameplay logic were untouched, exactly as scoped.
- Blockers/risks found: None. `eggAlgorithm.js` and `drawEnemy.js` untouched.
- Live verification (Chrome, dev server already running on :5173, account "โชแปง", reached via Home → "🗺️ ออกสำรวจ!"): with the flag at its `true` default, the map area renders a full diamond-tiled iso floor (green diamonds with visible top-left highlight edges, dark background filling the rest of the viewport) — confirms the projection math and painter's-algorithm draw order. Zero console errors. Flipped `window.__kq_isoDebug = false` live via the JS console and re-entered the world screen: the original flat top-down renderer (grid tiles, path, trees, player sprite, a wandering enemy) rendered correctly, confirming the fallback path is byte-for-byte unaffected — an enemy even walked into the player and correctly triggered the existing battle-select screen, proving collision/battle logic downstream of rendering is untouched. Flag set back to `true` before finishing. `npm run build` clean (172 modules).
- Ready to start next: Stage 2 — per-tile-type iso shapes (trees as raised/pointed diamonds or blocks, water with the existing frame-based color animation, walls, tall grass, signs, chests) replacing the flat `C.GRASS` fill in `drawIsoTile`/`renderMapIso`.
- Needs Chatbot decision first: Nothing blocking for Stage 2 — it's a direct continuation of this stage's scope per the original 6-stage spec.

**2026-07-02 — Audio expansion: new SFX/BGM, sound toggle removed:**
- Built: Expanded `src/lib/audio.js` per a task spec at `docs/HANDOFF_audio_expansion.md`. (1) 9 new SFX added to the `SFX` dict: `coin_earn`, `coin_purchase`, `item_equip`, `furniture_place`/`furniture_remove`, `room_visit_enter`, `minigame_start`, `lives_empty`, `unlock_new` — all raw Web Audio synthesis matching the existing style, wired to their real call sites (Collection.jsx purchases/equip, Room.jsx place/remove, RoomVisit.jsx mount, Home.jsx minigame launch, each minigame's lives-exhausted message, Home.jsx's unlock-threshold detection). (2) 3 new BGM tracks (`room`/`shop`/`minigame`) added to `BGM_TRACKS`, wired: `room` on Room.jsx mount, `shop` on Collection.jsx mount, `minigame` right before navigating into a launched minigame. (3) The pre-existing but never-called `victory` BGM track now has a real call site — `showVictory()` in `useBattleCombat.js`. (4) Coin-sound plumbing: added `dispatchAddCoins(dispatch, amount, bonusKey)` to `StateContext.jsx` — plays `coin_earn` then dispatches `ADD_COINS`; migrated all ~17 existing `ACTIONS.ADD_COINS` call sites (GameThai/GameMath/GamePhonics/GameShop/GameMathBattle/WorldBattle + all 5 minigames) to use it instead of dispatching directly, so every coin award plays its sound with no per-site SFX call needed; `ACTIONS.DAILY_LOGIN` (separate action, awards coins in its own reducer case) plays `coin_earn` at its own dispatch site. (5) Sound toggle fully removed: `setSoundOn()`/`toggleSound()`/`getSoundOn()` exports deleted from audio.js, `_soundOn` is now a `true` constant (existing `if (!_soundOn) return` guards left in place — harmless no-ops, kept the diff minimal and every existing sound function byte-for-byte unchanged); `soundOn`/`toggleSound` state + prop chain removed from `App.jsx` → `Home.jsx`/`GameScreen.jsx`; `kq_sound` localStorage key no longer read/written; 🔊/🔇 button removed from Home's header (GameScreen never actually rendered a toggle button itself — it was only forwarding an unused prop down into GameThai/GameMath/GamePhonics, confirmed dead before removing).
- `unlock_new` trigger — the spec flagged this as the one requirement without an obvious existing hook. Solved it in `Home.jsx`: a ref seeded to the current `unlockedGames(eggLevel)` set on first render (so nothing looks "new" right after loading the app), re-compared whenever `eggLevel` changes; if the set grew, play `unlock_new`. This is deliberately session-scoped, not persisted — reloading the app never re-fires it for a threshold already crossed in an earlier session. Flagging as a judgment call in case a persisted "first time ever" semantics is wanted instead (would need a new state field + defaultState migration).
- Victory-BGM scope decision: only wired into `useBattleCombat.js`'s `showVictory()` (used exclusively by `WorldBattle.jsx` via `MoveSelectBattleMode.jsx`), not into `GameMathBattle.jsx`'s separate, simpler battle system — that one has no existing BGM infrastructure at all (no `playBGM('battle')` call anywhere in it), so adding victory BGM there alone would be inconsistent (music starts only at the end). Flagging in case Chatbot wants battle BGM added to GameMathBattle too as a separate task.
- Not finished: Nothing outstanding from the spec — all sections (new SFX, new BGM, victory BGM, sound-toggle removal) are complete and wired.
- Blockers/risks found: None. `eggAlgorithm.js` untouched; phonics audio (`phonicsAudio.js`) untouched; no other screen logic changed.
- Live verification (Chrome, running dev server, account "โชแปง", coins bumped via localStorage for shop-affordability testing): shop purchase (BUY_ITEM, coins deducted, item auto-equipped) — no errors; cosmetic equip/unequip toggle (EQUIP_ITEM) — no errors; furniture buy → place → remove in Room (PLACE_ROOM_ITEM/REMOVE_ROOM_ITEM, toasts confirmed) — no errors; RoomVisit overlay opened from FriendsScreen's mystery-adventurer list (room_visit_enter mount) — no errors; minigame launch from Home's shortcut card (minigame_start + BGM, navigated correctly) — no errors; **full EggMemory playthrough to completion** (deterministic via a temporary `Math.random` patch to solve the shuffle) triggering a real `dispatchAddCoins` coin award (+12 coins, toast shown, balance updated) — zero console errors throughout. Could NOT verify: battle-victory BGM live (couldn't get world-map character movement working via the automated browser's D-pad/keyboard input within a reasonable time budget to trigger an actual battle win) — verified by code review only (single-line addition alongside the existing, already-working `playTone('fanfare')`/`playSFX('victory')` calls in the same function). Actual audio playback itself (whether it sounds correct/pleasant) cannot be verified by this agent at all — only that every call site executes without throwing. `npm run build` clean (zero errors) both before and after doc updates. Screenshot-confirmed no 🔊/🔇 button anywhere (Home header, GameScreen header).
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); or adding battle BGM to GameMathBattle.jsx if the victory-BGM scope decision above should be widened.
- Needs Chatbot decision first: Is the session-scoped (non-persisted) `unlock_new` trigger acceptable, or should it be "first time ever" (would need a new persisted state field)? Should `GameMathBattle.jsx` get its own battle/victory BGM to match WorldBattle, or is that out of scope?

**2026-07-02 — Minigame daily-lives + unlock-gating system:**
- Built: This session picked up a prior session's uncommitted work-in-progress (found already sitting in the working tree — `src/lib/minigameLives.js` plus edits to all 5 minigames, `Home.jsx`, `state.js`, `StateContext.jsx`) and finished it. `src/lib/minigameLives.js` is now the single source of truth for per-game daily lives + unlock gating: `MINIGAMES` config (world key, state field names, `max` lives/day, `unlockLevel` vs. companion `battleLevel`, deduct-life reducer action, title), `livesRemaining()`, `unlockedGames()`, `heartsStr()`. Each of the 5 minigames (Memory 5/day always-unlocked, Catch 5/day@Lv2, Run 3/day@Lv6, Tower 5/day@Lv6, Fishing 3/day@Lv10) has a `ready` phase (hearts row + start button, or a locked-out message) before play; starting deducts one life via its own `*_DEDUCT_LIFE` reducer case (all stamp `lastSavedAt`, per the existing resolveSync-safety pattern). Home's minigame shortcut card no longer hardcodes Egg Memory — it now picks randomly among unlocked games with lives left and shows the pooled total. This resolves the "Minigame picker screen" item that was flagged as needing a Chatbot decision (2026-07-01 Home redesign session) — went with the lightweight random-pick MVP rather than a dedicated picker screen or map-tile routing; **flagging to Chatbot in case a real picker screen or different unlock thresholds are wanted instead** (see below).
- Bug found + fixed: while finishing the migration, found `EggRun.jsx` still had its own inline lives-reset check comparing `state.lastRunDate` (stamped by the reducer using the shared `todayStr()` helper, e.g. `"2026-7-2"`) against `new Date().toLocaleDateString()` — a different format that never matches. This meant EggRun's own ready screen was permanently stuck showing 3/3 hearts no matter how many runs were actually left (Home's shortcut already had the correct count, since it always used `minigameLives.js`/`todayStr()` — only EggRun's own in-game screen was wrong). Migrated EggRun to the shared module, closing the gap. Confirmed live: forced a same-day partial-lives state via localStorage and verified EggRun's ready screen now shows the correct partial count instead of a false full reset.
- Also rebalanced coin-reward formulas across all 5 minigames (more tiers / higher variance vs. the old flat 3-tier caps) — was already partially done in the picked-up work-in-progress; left as-is since it wasn't flagged as broken and reads as intentional balancing, not scope creep.
- Not finished: Nothing outstanding in this feature — all 5 minigames + Home shortcut verified live in Chrome (correct hearts per game, pooled total agrees, zero console errors), build clean (172 modules).
- Blockers/risks found: None. Note the unlock gating uses companion **battle level** (single-egg model), not the old "N hatched eggs" scheme `CURRENT_STATE.md` used to document pre-companion-migration — doc updated to match. `GameScreen.jsx` itself still has zero unlock enforcement (renders whatever `currentWorld` is set to); Home's shortcut is the only gate, same as before this session.
- Ready to start next: Phase 4 NPC System; content expansion (Thai/Math/English); or a real minigame picker screen if Chatbot decides the random-pick MVP isn't sufficient.
- Needs Chatbot decision first: Is the random-pick-from-unlocked Home shortcut sufficient, or should there be a dedicated "choose a minigame" screen showing all 5 with their own lives (some kids may want to pick a specific game, not get a random one)? Are the chosen unlock levels (2/6/6/10) and lives/day (5/5/3/5/3) reasonable, or should they be tuned?

**2026-07-01 — Friends room visit + cosmetics:**
- Built:
  - **RPC** — new migration `supabase/migrations/20260701_mystery_adventurers_room_visit.sql`: `CREATE OR REPLACE`s `get_mystery_adventurers` (via DROP+CREATE, return TABLE gains columns) to add `equipped_head text` / `equipped_face text` / `room_layout jsonb`. Real rows read `eggs.state_json->'equipped'->>'head'`/`->>'face'` + `COALESCE(state_json->'roomLayout','{}')`. Bots get plausible random cosmetics (~35% empty per slot → mixed both/one/neither) + a 0–3-entry random room layout (item ids match `COSMETIC_ITEMS` / `ROOM_ITEMS`). Privacy unchanged (no child_name/user_id). Pre-change backup saved to `db_backups/get_mystery_adventurers_pre_room_visit.sql`.
  - **Shared helper** — `src/lib/roomScene.js` `drawRoomScene(ctx,{W,H,roomLayout,small,hint})` extracted from `DecoratedRoom.jsx` (which now delegates to it). `small` → thumbnail scaling; `hint=false` → suppress the "decorate at Room menu" text for friend rooms. Large+hint path is pixel-identical to the old scene.
  - **New components** — `src/components/RoomScene.jsx` (canvas: room + optional in-room egg via `renderEggSprite`, decoupled from local player state); `src/components/RoomVisit.jsx` (full-screen slide-in overlay: header + back button, full-bleed room + centered `EggCanvasCore` + bottom stats/cosmetic-chips panel or "ยังไม่ได้แต่งตัว").
  - **FriendsScreen.jsx** — mystery-adventurer cards: left 72×80 room thumbnail + right name/rarity/element·Lv/HP-ATK-SPD/inline cosmetic icons (no text/count). Card-tap → RoomVisit; "ดูสเตตัส" (stopPropagation) still opens the old stats modal + "ท้าเล่น" mock flow.
- 🔴 **Needs USER action — RUN THE MIGRATION MANUALLY**: paste `supabase/migrations/20260701_mystery_adventurers_room_visit.sql` into the Supabase SQL Editor and run it (https://supabase.com/dashboard/project/dgpsnlkedergkbhqnjpu/sql). No Supabase CLI / service key in repo → Claude Code CANNOT apply it. Until then the RPC returns the old shape and the UI degrades gracefully (empty rooms, no cosmetics) — this is exactly what live testing showed and is expected.
- Not finished / degraded until migration: live browser test showed empty rooms + no cosmetics for all adventurers (the live DB still runs the old RPC). Card layout, room thumbnails, room-visit overlay, back button, and the preserved ดูสเตตัส/ท้าเล่น modal all verified working; cosmetics + furniture will populate once the migration is run.
- Blockers/risks found: None. The migration is a full superseding definition, so running 20260701 alone brings the RPC fully current (no need to run 20260627 first).
- Live verification (Chrome, account "โชแปง"): เพื่อน → ผู้คนอื่นๆ renders 8 cards with mini room previews + stats (no crash); tapping a card opens the centered room-visit screen; back button returns to the list; ดูสเตตัส opens the stats modal ("ท้าเล่น" intact); Home + Room editor visually unchanged after the DecoratedRoom refactor; zero console errors. Build clean.
- Ready to start next: Phase 4 NPC System; or content expansion (Thai/Math/English).
- Needs Chatbot decision first: Nothing blocking. (Optional future: real friend-vs-friend visit uses the SAME RPC path, but visiting a *specific* accepted friend by id would need a new RPC — out of scope; current screen shows the anonymized mystery-adventurer pool only.)

**2026-07-01 — Companion egg enlarged + centered on Home:**
- Built: Follow-up fix to the same-day Home redesign — the redesign left the companion visible only as the tiny 48px room-walker sprite, which didn't read as "the main character." Gave `DecoratedRoom.jsx` a `showWalker` prop (default `true`, so `Room.jsx`'s call site — which doesn't pass it — is completely unaffected and still shows its small walker + placement grid exactly as before). Home now passes `showWalker={false}` so `DecoratedRoom` draws only the room background/furniture each frame (walker's RAF update/draw skipped via a ref-read flag). On top, added a large centered 200×236 `EggCanvas` reusing the pre-redesign large-egg pipeline (re-added `cssAnimToEggAnim`/`cssAnimToMood` helpers for anim/mood/glow), tappable via the existing egg-zone `onClick`, with a scale-bounce on `creatureBounce`. Equipped cosmetics show automatically (the `EggCanvas` wrapper defaults to `state.equipped`). Repositioned the floating reaction/heal-float (now above the egg's head, `calc(50% - 138px)`) and tap particles (now centered on the egg, `50%`) — both were tuned for the old tiny-walker position.
- Not finished: Nothing — this was a small, complete visual fix.
- Blockers/risks found: None.
- Live verification (Chrome, running dev server, account "โชแปง"): large glowing egg renders centered over the room art; tap-to-pet confirmed working (expression changed to happy, aura shifted to warm gold glow); Room screen re-checked and still shows its walker + grid unaffected. Build clean (168 modules).
- Ready to start next: Phase 4 NPC System, content expansion, or the minigame-picker-screen decision flagged in the prior Home redesign session.
- Needs Chatbot decision first: Nothing blocking.

**2026-07-01 — Home screen redesign:**
- Built: Rewrote `src/components/Home.jsx` to the approved layout — (1) simplified header: small circular companion-egg avatar + child name + egg-stage name (whole block taps to Profile/Login), 🔥 login-streak pill, 🪙 coin pill, 🔊/🔇 sound toggle; removed the pulsing "พร้อมฟัก!" badge and the old text login/profile button. (2) New thin status bar with three chips ❤️ HP / ⭐ XP / 💕 Bond (proportional bars, no numbers) — **Bond meter shown on Home for the first time**. (3) `<DecoratedRoom>` promoted from the full-viewport `zIndex:-1` background into the flex-1 hero zone so the walking companion egg is actually visible in the middle; floating "Lv.N · [stage name]" pill top-center; DecoratedRoom internals untouched. (4) Egg-tap-to-pet preserved via a transparent tap target (armed item → use; post-hatch → +bond & floating emoji; pre-hatch → pet/hatch). (5) Party bar removed. (6) Compact evolution-progress bar removed from Home. (7) Pre/post-hatch JSX branch split collapsed into one layout. (8) Minigame shortcut card added. (9) Item tray restyled (bigger icons + status line) with all logic unchanged. (10) Full-width "🗺️ ออกสำรวจ!" button; old "ลูบ!" button removed. No state shape / reducer / other-screen changes; `eggAlgorithm.js` untouched. Docs updated (CURRENT_STATE, TASKS, CHANGELOG).
- New-player zero-egg case: preserved the hatch flow. When `readyToHatch && hatchedEggs.length===0`, a pulsing "แตะเพื่อฟักไข่!" CTA renders in the egg zone and tapping the egg fires `SET_HATCHING` (via `handlePetEgg`). The single simplified layout reads `activeEgg` with `?.`/fallbacks (HP 100%, Bond 0%) so a genuinely new account renders and can still hatch — onboarding for future accounts is intact. Verified by code (the live test account already has hatched eggs; I did not zero out its `hatchedEggs` because that risks the documented resolveSync-revert bug + leaving test data).
- Minigame shortcut destination: launches **Egg Memory** directly (`SET_CURRENT_WORLD:'memory'` + `navigate('game')`). Found that **no minigame-picker screen exists** and, more surprisingly, GameScreen + all 5 minigames were **completely unreachable** before this change — `SET_CURRENT_WORLD` was dispatched nowhere and `navigate('game')` appeared nowhere; Phase 6 map-tile launches are unbuilt. Building a real picker is out of scope for "redesign Home.jsx", so I did the minimal 2-line wiring to the first-listed, dependency-free game.
- Live browser verification (Chrome vs running dev server, logged-in account "โชแปง"): layout matches spec; egg-tap pet works; arm-food → tap-egg → use decremented 172→171; minigame card → Egg Memory grid; explore → Green Meadow map; bottom nav + Shop + Room screens visually unaffected; zero console errors. Build clean (168 modules).
- Not finished: Zero-egg render not exercised live (reasoned via code, see above). Egg-tap pet + minigame launch have no unlock gating (GameScreen renders whatever `currentWorld` is set to) — fine for now.
- Blockers/risks found: None blocking. Scope-guardian flag raised on the minigame picker (see below).
- Ready to start next: Phase 4 NPC System; or Thai/Math/English content expansion.
- Needs Chatbot decision first: **Minigame picker screen** — should there be a dedicated "choose a minigame" screen (with unlock gating per CURRENT_STATE), or should minigames be reached via map tiles (Phase 6)? The Home shortcut currently hardcodes Egg Memory as a placeholder destination.

**2026-07-01 — Sweep: lastSavedAt on every mutating reducer:**
- Built: Follow-up to the previous two resolveSync-revert fixes (`c74e83d` for coins, `036070f` for cosmetics/room items). The prior session flagged that `ADD_COINS` and other reducers likely shared the same latent gap. Did a full audit of `StateContext.jsx`'s ~70 `case ACTIONS.*` reducers and added `lastSavedAt: Date.now()` to every case that returns a genuinely mutated state — XP/round/hatch, home/battle items, mastery/level/subject progress, egg-run scores, happiness, battle records, profile, shopV1/session/response-time logs, world/screen navigation, party/battle-creature flow, creature HP/bond/evolution/stats, boss/maze state, pending level-up/rewards, and `ADD_COINS`. Excluded only `INIT` (must preserve the loaded snapshot's own `lastSavedAt`, not stamp a fresh one), the true no-op `ENCOUNTER_TRIGGERED`, the `USE_ITEM`/`DROP_ITEM` aliases (they delegate via recursive `reducer()` calls into already-stamped cases), and the six reducers `036070f` already fixed.
- Verification: wrote a `+5` coin bump to `localStorage.kq_state` with a fresh `lastSavedAt` (mirroring the patched `ADD_COINS` reducer's exact output shape) against the real running app/account, reloaded, confirmed it survived (11→16 in the HUD), then restored the account to 11 coins so no test data was left behind. Build clean (168 modules). Wrote a small Node script to scan every `case` block and confirm none were missed (only INIT/ENCOUNTER_TRIGGERED/aliases lack the stamp, as intended).
- Not finished: Did not touch `resolveSync()` itself or attempt a field-level merge — kept the fix minimal and consistent with the established per-reducer stamp pattern, per the previous session's own judgment that a resolveSync rearchitecture would be over-engineering for this defect class.
- Blockers/risks found: None. This should be the durable, permanent close of this bug class — but note any *future* reducer added to `StateContext.jsx` that forgets to add `lastSavedAt` will silently reintroduce it (there's no lint/test enforcing this). Worth flagging to Chatbot if this recurs a third time — a small unit test asserting every `case` branch's output includes `lastSavedAt` would catch it automatically.
- Ready to start next: Phase 4 NPC System, or content expansion (Thai/Math/English).
- Needs Chatbot decision first: Nothing blocking. Optional: should a lint/test guard against future reducers missing `lastSavedAt`? Flagging as a suggestion, not doing it unprompted (would be scope creep beyond this sweep).

**2026-07-01 — fix: bought cosmetics disappear after close/reopen (resolveSync race):**
- Built: Fixed the live-reported bug where a bought cosmetic (e.g. a bow) showed as owned in-session but was gone after a full close/reopen (shop re-prompted to buy). Root cause was the EXACT same class as `c74e83d`: `BUY_ITEM` and `EQUIP_ITEM` (`StateContext.jsx`) updated `ownedItems`/`equipped` but never bumped `lastSavedAt`, so the in-memory state kept the previous session's timestamp. A later `resolveSync` (mount `loadState().then` at `:1009`, or `SIGNED_IN` listener at `:1059`) compared the frozen local timestamp against a stale remote Supabase snapshot, hit the `remoteTime >= localTime` tie rule, picked remote, and dispatched `INIT` — reverting the purchase and then persisting the reverted state everywhere. Fix = the same one-liner as `c74e83d`: added `lastSavedAt: Date.now()` to `BUY_ITEM` + `EQUIP_ITEM`, and to the room reducers (`BUY_ROOM_ITEM`, `PLACE_ROOM_ITEM`, `REMOVE_ROOM_ITEM`) which had the identical latent gap. `migrateStateShape()` was NOT the cause (it preserves both fields via spread). No resolveSync rearchitecture — matched the existing pattern.
- Not finished: The buy-through-UI click was not exercised live (save has 11 coins, cheapest cosmetic is 30 — same precedent as the furniture session). Buy step is code-verified; the persistence/resolveSync half was live-verified.
- Blockers/risks found: `ADD_COINS` and round/battle coin-award reducers likely share the same latent no-`lastSavedAt`-bump gap; not observed reverting in practice (they usually sync before the next resolveSync) and out of this task's scope, but worth a sweep if any "earned X reverted" report appears. The real fix is a per-mutation `lastSavedAt` stamp on EVERY mutating reducer, or making `resolveSync` field-merge — deferred as it's not currently biting.
- Ready to start next: Optional audit/sweep of all mutating reducers to stamp `lastSavedAt` uniformly (prevents this whole bug class); or Phase 4 NPC System.
- Needs Chatbot decision first: Nothing blocking.

**2026-07-01 — World-map cosmetics + dead code cleanup:**
- Built: (1) `WorldScreen.jsx`'s `window.__kq_companionEgg` now includes `equipped: state.equipped ?? null`, closing the deferred item from the previous two sessions. `tileEngine.renderPlayer` (`tileEngine.js:242`) already spreads the whole companion object into `renderEggSprite`, so no change was needed there — the existing `drawCosmetics` step (added for the Home room-walker fix) now also draws on the 16×16 map walker automatically. (2) Deleted `src/components/HomeBackground.jsx` (confirmed zero remaining imports; `Home.jsx` uses `DecoratedRoom`) and cleaned up a stale reference in `Home.jsx`'s header comment.
- Not finished: Live browser re-verification of the map-walker cosmetics was not performed this session — the logged-in Chrome tab from the prior task's live-testing pass had already closed by the time this task started, and re-doing the login flow for a 2-line, pattern-matched change (identical to the already-live-verified `DecoratedRoom.jsx` fix) wasn't judged worth another login round-trip. Verified via clean `npm run build` (168 modules, zero errors) + direct code read confirming the spread propagates `equipped` correctly.
- Blockers/risks found: None. `CURRENT_STATE.md` had several stale references to `HomeBackground.jsx` from before it was deleted (some going back to the original creature→egg migration doc pass); updated the ones describing current live behavior, left older architecture-log prose otherwise alone (out of scope for this pass).
- Ready to start next: A live click-through of the world map to visually eyeball cosmetics on the walker sprite would close the loop (someone should just log in and look). Otherwise: Phase 4 NPC System, or a `CURRENT_STATE.md` "Home Screen" section rewrite (that section still describes the pre-DecoratedRoom sky/mountains scene and legacy `drawCreature` zone — needs a fuller pass beyond this session's scope).
- Needs Chatbot decision first: Nothing blocking.

**2026-07-01 — Furniture tab CSS collapse fix:**
- Built: Fixed the real (live-confirmed) reason the shop furniture tab was unreachable — a CSS flexbox min-size collapse, NOT the data/import issue that the 2026-06-30 changelog entry and a later static audit claimed. The top-level 👗/🏠 tab-switcher div in `Collection.jsx` sets `overflow:hidden` (to clip rounded corners) and lives inside the outer flex-column (`overflowY:auto`, height:100%). Per flexbox's automatic-min-size rule, an `overflow:hidden` flex item gets min-height 0, so with page content taller than the viewport the shrink algorithm collapsed it 42px→2px — both tab buttons became an invisible, unclickable sliver, so a real pointer could never reach the furniture tab (only a scripted JS `.click()` could, which is why every prior static/scripted check "passed"). Fix: added `flexShrink:0, flexGrow:0` to that div. Found + fixed a SECOND identical instance (also live-verified at 2px): the Head/Face (หัว/หน้า) sub-tab bar in the wearable section — same pattern, same one-line fix (2px→38px). Live-verified in Chrome vs the running dev server: reloaded, real-clicked 🏠 เฟอร์นิเจอร์, all 12 furniture items rendered; try-on preview (party hat on egg + tags) and Room (ห้อง) tab both still work — no regressions.
- Not finished: The live buy-flow click was NOT exercised — the save has 11 coins, cheapest furniture is 30. Did not manipulate coins/localStorage. Buy logic instead verified by reading `handleBuyFurniture` + `BUY_ROOM_ITEM` reducer (guards insufficient coins + duplicate ownership, deducts price, appends to `ownedRoomItems`) — correct.
- Blockers/risks found: The general anti-pattern (`overflow:hidden` on a shrinkable flex item inside a flex-column) can recur anywhere. Checked `Collection.jsx` + `Room.jsx`; only the two tab bars were affected (Room.jsx line 150 already had `flexShrink:0`; its item strip uses `overflowX:auto` horizontal scroll — fine). Worth remembering when adding future segmented controls.
- Ready to start next: Thread `equipped` into the world-map player sprite for cosmetics parity; or Phase 4 NPC System.
- Needs Chatbot decision first: Nothing blocking.

**2026-07-01 — Shop live try-on preview:**
- Built: `Collection.jsx` cosmetics tab now has a large companion preview egg at the top (real element/eye/gender/stage/aura via `eggProgressData.stage` + `stageToAura`). New local-only state `preview: {slot,id}|null` drives `previewEquipped = preview ? {...equipped,[slot]:id} : undefined`, passed as the big egg's `equipped` prop (undefined → wrapper falls back to real `state.equipped`). Tapping ANY card is instant: unowned → `preview` only (free, no dispatch, no coins), with a "👀 ลองใส่" tag over the big egg + "👀 กำลังลอง" per-card and a "ซื้อ 🪙price" buy button; owned → real `EQUIP_ITEM` toggle (reducer contract unchanged) + clears preview. Preview resets on unmount / buy / real equip / top-tab switch, so leaving the shop never persists an unbought try-on. Also resolved the deferred walker-cosmetics item: `renderEggSprite.js` gained an `equipped` param + `drawCosmetics` step, and `DecoratedRoom.jsx` threads `state.equipped` into its companion ref → the Home-background room walker now shows hats/glasses.
- Home check: Home's large egg-zone `EggCanvas` (both eggsHatched>0 and ==0 branches) already rendered real equipped cosmetics — the app-level wrapper defaults `equipped` to `state.equipped`. No fix needed there; only the DecoratedRoom walker was missing cosmetics and is now fixed.
- Not finished: The WORLD MAP player sprite (`tileEngine.renderPlayer` via `window.__kq_companionEgg`, set in `WorldScreen.jsx`) still does NOT pass `equipped` to `renderEggSprite`, so cosmetics don't show on the map walker — out of this task's Home-focused scope. One-line follow-up if wanted.
- Blockers/risks found: Live browser verification blocked — no Chrome extension connected AND App gates all screens behind Supabase login (`App.jsx:84`, no guest bypass at the gate). Did not enter credentials. Verified via clean `npm run build` (168 modules) + a Node reducer/preview data-flow trace confirming: unowned tap never spends/persists, leaving resets to prior `state.equipped`, buy deducts+equips+drops preview, owned tap does real toggle, cross-slot preview keeps other equipped items, insufficient coins is a no-op.
- Ready to start next: Thread `equipped` into the world-map player sprite (WorldScreen `__kq_companionEgg` → renderEggSprite) for full cosmetics parity; or Phase 4 NPC System.
- Needs Chatbot decision first: Should map-walker cosmetics be added too (visual clutter at 16×16)? Otherwise nothing blocking.

**2026-06-30 (session 10) — Decorated room as Home background:**
- Built: `src/components/DecoratedRoom.jsx` — single-canvas room renderer: cream/wood gradient background, 12 furniture slots drawn via `item.draw()`, entity state machine (walk/idle/jump/spin) with `renderEggSprite` for the companion walker — all inside the room's floor area. Reads `state.roomLayout` via a ref updated every render so the RAF loop always sees the latest placement. Uses `useAppState()` + `useCompanion()` internally (self-contained). `Home.jsx`: `<HomeBackground>` removed, replaced with `<DecoratedRoom style={{ position:'absolute', inset:0, zIndex:-1 }}>` — Home background now shows the decorated room. `Room.jsx`: inline room scene (CSS gradient + EggCanvas) replaced with `<DecoratedRoom style={{ position:'absolute', inset:0 }}>` as visual base; transparent CSS grid overlay (12 slot divs, dashed/solid borders) layered on top as pure tap targets; `Slot` component removed; `EggCanvas` import removed; `SlotCanvas` + `ItemThumb` kept for picker modals. `HomeBackground.jsx` left in place (no longer imported). Build clean at 168 modules.
- Not finished: Nothing — spec complete. HomeBackground.jsx can be deleted in a future cleanup session.
- Blockers/risks: None.
- Ready to start next: Walker cosmetics (thread `state.equipped` into `renderEggSprite.js` so room walker + map walker show hats/glasses); or Phase 4 NPC System.
- Needs Chatbot decision first: Should the walker also show cosmetics (hats/glasses)? If yes, code path is clear.

**2026-06-30 (session 9) — Room / Den decoration system:**
- Built: `src/lib/roomItems.js` — 12 pixel-art furniture items (plant, rug, lamp, stuffed animal, window with curtains, small chair, desk, toy chest, bookshelf, wall art, bed, fish tank). Each `draw(ctx, cx, cy, sz)` draws a canvas-based pixel-art icon (no paths — fillRect only, consistent with egg cosmetics). Tiers: small 30–60 / mid 150–280 / big 500–600. `src/components/Room.jsx` — full Room screen: warm cream+wood room background (CSS gradient), 4×3 grid of 64px slots for placing furniture, companion EggCanvas standing in the lower center, tap empty slot → picker bottom sheet (owned-but-unplaced items), tap occupied slot → remove/swap action sheet, toast feedback. `src/components/Collection.jsx` — extended with top-level tabs (👗แต่งตัว | 🏠เฟอร์นิเจอร์); furniture tab shows 12 items with warm room-background preview, buy flow uses `BUY_ROOM_ITEM`. `src/components/BottomNav.jsx` — 5th tab "ห้อง" added (yellow→purple→green→blue→orange). `src/App.jsx` — added `screen === 'room'` + Room import. `src/lib/state.js` — `ownedRoomItems: []` + `roomLayout: {}` added to `defaultState()`. `src/context/StateContext.jsx` — `BUY_ROOM_ITEM` (deduct coins, add to ownedRoomItems), `PLACE_ROOM_ITEM` (set roomLayout[slotIndex]=itemId), `REMOVE_ROOM_ITEM` (delete slot from layout) — all 3 reducer cases. Build clean at 168 modules.
- Not finished: Nothing — spec complete. Multi-room expansion is future scope.
- Blockers/risks: None.
- Ready to start next: Phase 4 NPC System; or walker cosmetics (renderEggSprite)
- Needs Chatbot decision first: Nothing — room system is self-contained for now.

**2026-06-30 (session 8) — Cosmetic items (head + face), shop, and wardrobe:**
- Built: `src/egg/eggCosmeticLayer.js` — 18 pixel-art cosmetic items (10 head: bow, party hat, beanie, cap, headband stars, flower crown, top hat, wizard hat, gold crown, jeweled crown; 8 face: blush, freckles, flower cheek, mustache, round glasses, eye mask, sunglasses, star glasses). Exported `COSMETIC_ITEMS` catalog (id/slot/nameTh/nameEn/price/tier/draw) and `drawCosmetics(ctx, o, equipped)`. Wired into `src/egg/index.js` barrel export. `src/egg/EggCanvas.jsx` (core): added `equipped` prop (default null); `drawCosmetics` called as step 9 (before flash, inside pose transform), `equipped` added to useEffect dep array. `src/components/EggCanvas.jsx` (wrapper): imports `useAppState`, reads `state.equipped` by default, accepts `equipped` prop override (for shop previews). `src/lib/state.js`: added `ownedItems: []` and `equipped: { head: null, face: null }` to `defaultState()` — backfilled automatically for existing players via `{ ...defaultState(), ...saved }` spread. `src/context/StateContext.jsx`: added `BUY_ITEM` and `EQUIP_ITEM` actions + reducer cases (BUY_ITEM: deducts coins, adds to ownedItems, auto-equips; EQUIP_ITEM: toggles equipped[slot] — tap again to unequip). `src/components/Collection.jsx`: replaced coming-soon placeholder with full shop UI — coin balance header, HEAD/FACE tab switcher, 2-column item grid with per-item EggCanvas preview (showing that item equipped), tier badge, buy/equip/unequip buttons, toast feedback. All screens using `<EggCanvas>` (wrapper) automatically show equipped items — no per-screen changes needed. Build clean at 166 modules.
- Not finished: `renderEggSprite.js` walker (map/home canvas) does not show cosmetics — drawing cosmetics on the non-React walker requires threading `equipped` through `renderEggSprite` and `useWorldGameLoop`/`HomeBackground`. Deferred.
- Blockers/risks: None.
- Ready to start next: Wire cosmetics into `renderEggSprite.js` walker (simple — pass `equipped` to `drawCosmetics` after expression step); or Phase 4 NPC System
- Needs Chatbot decision first: Should the walker (map player sprite + home walker) also show hats/glasses? If yes, code path is clear and ready to implement.

**2026-06-30 (session 7) — Coins earned shown on all result screens:**
- Built: All 16 result/end screens now display the exact coins earned that round. `GameThai.jsx`: `useFinishRound` refactored to return `{ finish, coins }` — coins pre-computed before the closure, passed as `coins` prop to `ResultScreen`. `GameMath`, `GameShop`, `GameMathBattle`: added `coinsEarned` state, set before dispatch, shown in inline done screens. `GamePhonics`: `ResultScreen` gains `coins` prop; all 4 sub-games (PhonicsGame L1, CVCGame L2, SightGame L3, SentenceGame L4) add `coinsEarned` state. Minigames: EggMemory/EggFishing hardcoded +5; EggTower/EggCatch display computed from `score` state (deterministic, same formula); EggRun badge in canvas dead overlay from `gsRef.ringCount`. WorldBattle: `pendingBattleCoins` state set alongside dispatch, passed to `<RewardChest coins={...}>`; RewardChest shows animated gold pill in reveal phase. Build clean at 165 modules.
- Not finished: Nothing — all screens covered.
- Blockers/risks: None.
- Ready to start next: Coin spending system (shop/wearable items) — Chatbot must define what coins buy first
- Needs Chatbot decision first: What can coins buy? What does the shop UI look like? Any daily coin cap?

**2026-06-27 (session 6) — Coin economy (earn-only foundation):**
- Built: Added `coins`, `lastLoginDate`, `loginStreak`, `coinsLevelBonus` to `defaultState()` + `migrateStateShape()` (existing players load with `coins: 0`). Added `ADD_COINS` and `DAILY_LOGIN` reducer actions. Hooked coin awards at: all ROUND_COMPLETE paths in GameThai (via `useFinishRound`), GameMath, GamePhonics (all 4 sub-games), GameShop, GameMathBattle; world battle win (+10 regular / +15 boss, in `WorldBattle.onComplete`); arcade minigame completions (EggMemory +5, EggTower score-scaled 3–8, EggRun ring-scaled 3–8, EggCatch score-scaled 3–8, EggFishing +5). Level-unlock first-time bonus (+15, guarded by `coinsLevelBonus[world_level]` flag — no re-award). Daily login (+10 + min(streak,5) each new calendar day). Coin HUD added to Home.jsx header (gold 🪙 badge). `showItemToast` used for all coin feedback. Formula: `round(12 × accuracy × (1-mastery))`, clamp [2,12]. Anti-farm: replaying mastered level → min 2 coins/round. Build clean at 165 modules.
- Not finished: No spending, no shop, no items linked to coins — earn-only as specified.
- Blockers/risks found: `showItemToast` in StateContext.jsx (daily login) uses 900ms setTimeout to let ItemToast component register. If app loads very slowly, toast might fire before registry. Acceptable for now.
- Ready to start next: Coin spending system (shop/wearable items) — next session after design from Chatbot
- Needs Chatbot decision first: What can coins buy? What's the shop UI? Any daily coin cap?

**2026-06-27 (session 5) — Legacy creature art removal STEP 2+2.5+§3+§4:**
- Built: Deleted BattleScreen.jsx, HatchOverlay.jsx, CreatureCanvas.jsx, drawCreature.js, creatureAlgorithm.js, creatureHelpers.js (all zero-caller verified). Replaced: LoginBackdrop.jsx → 9 random-element egg RAF sprites (renderEggSprite); EggMemory.jsx → 6 emoji card pairs (🔥💧⚡🌿🌑✨, no creature dependency). Removed dead WorldHUD globals (__kq_activeCreatureSeed/__kq_activeCreatureStats + getCreatureSeed import). Removed dead HatchOverlay import/JSX from App.jsx. Removed dead getCreatureForHatch import from StateContext.jsx. Created db_backups/get_mystery_adventurers.OLD.sql with backup note + retrieval instructions. Build clean at 165 modules.
- Not finished: `get_mystery_adventurers` RPC migration still not applied (supabase/migrations/20260627_mystery_adventurers_egg.sql must be run in Supabase SQL Editor). STEP 3 (DB column/RPC-body drops) not started.
- Blockers/risks found: The db_backups file is a placeholder — user should run `SELECT pg_get_functiondef('get_mystery_adventurers'::regproc)` in Supabase SQL Editor and save the real definition before applying the migration.
- Ready to start next: Apply the RPC migration SQL in Supabase SQL Editor → test FriendsScreen Mystery Adventurers tab; then Phase 4 NPC System
- Needs Chatbot decision first: STEP 3 (actual DB column drops on hatchedEggs blob fields) — user must give explicit OK after verifying backup

**2026-06-27 (session 4) — Full-pipeline animated walkers + Mystery Adventurers egg upgrade:**
- Built: Created `src/egg/renderEggSprite.js` — shared non-React helper running the full 9-step egg compositing pipeline (aura→pose→regalia→body→eyes→expression). Updated `HomeBackground.jsx` to call `renderEggSprite` per-frame into a reused 48×48 offscreen (`basePxOverride=2`); element animations are now live. Updated `WorldScreen.jsx` to set `window.__kq_companionEgg` (not a baked canvas); updated `tileEngine.renderPlayer` to call `renderEggSprite` per-frame into a 32×32 offscreen scaled to 16×16. Removed `drawCreature` from tileEngine. Updated `FriendsScreen.jsx` MysteryTab: `<EggCanvasCore>` per adventurer row/modal; removed `CreatureCanvas`/`ELEMENT_STATS`/`elementToStats`/`drawCreature`. Generated `supabase/migrations/20260627_mystery_adventurers_egg.sql` for new RPC returning `element/eye/gender/stage/...`. Build clean at 170 modules.
- Not finished: Supabase migration for `get_mystery_adventurers` NOT yet applied — adventurers will show default egg (fire/gba/male/stage1) until migration runs
- Blockers/risks found: Migration must be pasted and run at https://supabase.com/dashboard/project/dgpsnlkedergkbhqnjpu/sql
- Ready to start next: Run the migration SQL, then test FriendsScreen in browser; or BattleScreen.jsx dead-code deletion; or Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-27 — Companion egg walker on Home; Collection placeholder:**
- Built: `HomeBackground.jsx` rewritten — single companion egg entity (walk/jump/spin) using `drawEggBody`+`drawEyeLayer` to 48×48 offscreen canvas; `Home.jsx` passes `companion={resolved}` + `stage` from `useCompanion()`; voice profile now derived from companion element/gender (no DNA). `Collection.jsx` replaced with "เร็วๆ นี้!" placeholder (companion EggCanvas + coming-soon copy). `CreatureDetailPopup.jsx` deleted (orphaned). Build clean at 169 modules.
- Not finished: none
- Blockers/risks found: `HomeBackground` now shows only 1 entity (the companion); the old "meeting gimmick" between multiple walkers is gone — intentional
- Ready to start next: FriendsScreen MysteryTab rework (replace `drawCreature` + `get_mystery_adventurers` with companion EggCanvas); or BattleScreen.jsx deletion (orphaned); or WorldHUD `getCreatureSeed` dead-code removal
- Needs Chatbot decision first: Full Collection redesign (shop/cosmetics/room) — placeholder only for now

**2026-06-26 (session 2) — Companion egg shown on all screens; name = child's account name:**
- Built: Replaced `drawCreature` canvas with `EggCanvas` on Home (large display + party bar), Collection (PartyGrid), PartySelect, and Battle (player side). Name `state.name` shown everywhere instead of `creatureName`. Map player sprite: WorldScreen pre-renders companion egg body+eyes to `window.__kq_playerOffscreen` (32×32); `tileEngine.renderPlayer` uses it. Build passes. Deployed to Vercel.
- Not finished: none — all 5 screens done
- Blockers/risks found: none
- Ready to start next: Phase 4 NPC System; or companion aura progression (aura shown based on stage)
- Needs Chatbot decision first: none

**2026-06-26 — Living Egg renderer + one-time Companion Creation:**
- Built: Full `src/egg/` layer system wired into a new `EggCanvas.jsx` React component (RAF loop, DPR-backed canvas, 11-step compositing pipeline). Created `CompanionContext` (loads `companions` row from Supabase, exposes `resolved`/`loading`/`createCompanion`). Created blocking `CompanionCreation` modal (element × 6, eye × 4 with mini previews, gender × 2; live full preview; confirm dialog; Thai UI). Replaced `src/components/EggCanvas.jsx` with a thin wrapper that reads from CompanionContext and accepts legacy `stats` prop — all existing callers untouched. Added companion gate to `App.jsx` (waits for `companionLoading` before showing loading screen; blocks on `!companion` after onboarding). Wrapped app with `CompanionProvider` in `main.jsx`. Created `supabase/migrations/20260626_companions.sql` with DDL + RLS + `create_companion` RPC.
- Not finished: Supabase migration NOT yet applied — must be run manually in the Supabase SQL Editor before companion creation works in production.
- Blockers/risks found: No Supabase CLI available, no service_role key — migration must be pasted and run by hand at https://supabase.com/dashboard/project/dgpsnlkedergkbhqnjpu/sql
- Ready to start next: Phase 4 NPC System (Prof Owl already wired; add remaining 4 NPCs from SCREEN_NPCS config)
- Needs Chatbot decision first: none

**2026-06-22 — baby-stage Minecraft voxel/cube aesthetic redesign:**
- Built: Pivoted all 4 baby draw functions from organic/Pokémon-style curves to Minecraft mob voxel style. Each type now has one unmistakable Minecraft-style face feature: FURRED = large 4×2 pig snout block with 2 nostril dots at face bottom (Minecraft pig); WINGED = 4×1 accent crest bar at face top + 2×1 accent beak at face bottom (Minecraft chicken), wings as 4×4 panels spanning canvas edges; SCALED = 2×3 side frill blocks at extreme head edges (x=0 and x=10) + 1×2 slit pupils + 3×3 tail block (lizard/turtle style); CHITIN = compound eyes in accent color protruding outside head (Minecraft spider red-eye style), three-segment widening body 6→8→10. All shapes are pure flat rectangles, bigger/fewer than before, with face as the dominant feature.
- Not finished: browser visual test (chrome extension unavailable)
- Blockers/risks found: none
- Ready to start next: teen+final stage Minecraft-style pass, or Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-22 — baby-stage silhouette redesign (block-attachment technique, corrected from diagonal-curve approach):**
- Built: Rewrote all 4 baby-stage draw functions in `creatureAlgorithm.js` using block-attachment silhouette technique derived from Pokémon reference images. Key correction: previous pass wrongly used staircase/diagonal pixels; reference sprites use almost exclusively rectangular blocks with at most 1px step offsets. Per-type changes — FURRED: ears now 2-wide × 3-tall proper ear blocks (was 3×1 thin horizontal bar), head 10-wide with 6-wide body for chibi proportion, tail is flat 3×2 rectangle (was staircase), 4 leg stubs clearly separated. WINGED: wings are pure 3×4 rectangular blocks attached to body sides with consistent 2-wide fill throughout all 4 rows (no taper), 2-stub bipedal legs. SCALED: head enlarged to 8×5 (was 6×4), side frills as 2×3 blocks protruding sideways, tail is straight 2×5 vertical rectangle (no staircase), right leg shifted to x=6 to avoid merging with tail at x=9. CHITIN: antennae shortened to 2 rows (head starts at y=2), compound eyes still protruding 1px outside head shell, 3-segment widening body 6→8→10 intact.
- Not finished: browser visual test (chrome extension unavailable during session)
- Blockers/risks found: none
- Ready to start next: teen+final quality pass, or Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-21 — baby-stage quality pass (Pokémon-reference-informed):**
- Built: All 4 baby draw functions redesigned with reference-quality pixel-art techniques. (1) DARK OUTLINE everywhere — every shape uses "draw dark shell 1px larger, then fill on top" so a 1px dark border appears around every body region. (2) HEAD-TO-BODY FLOW — FURRED: 10-wide head shell → matching-width shoulder bridge row (primary, y=6) → body steps in 1px each side; WINGED: 8-wide head steps to 6-wide body at y=6; SCALED: head and body share same x-range so outline runs continuously; CHITIN: each segment's dark shell is 2px wider than the one above (6→8→10) creating a staircase widening. (3) DIAGONAL PIXELS — FURRED tail = 2-step stair-step arc; WINGED wing nubs = 3×3 dark block with fill tapering from 2-wide to 1-wide (left/right edges step diagonally); SCALED tail = straight column then 1-px rightward bend. (4) EYES — all 4 types now draw 2×2 dark eye block with explicit `r(x,y,1,1,'#ffffff')` white shine at upper-left. Chitin compound eyes moved to x=2/x=9 (1×2 tall, truly outside the head dark shell). Scaled baby spot/stripe overlay coordinates updated (old positions landed on dark-border row y=8 and leg zone y=9).
- Not finished: teen and final stage quality pass (baby approved first)
- Blockers/risks found: none
- Ready to start next: teen+final stage quality pass (same 4 techniques, extend to the other 8 draw functions) — OR — Phase 4 NPC System
- Needs Chatbot decision first: which to do next — creature quality pass on remaining stages, or move to NPC system?

**2026-06-20 — creatureAlgorithm.js silhouette redesign (round 4 — chibi pixel-art principles):**
- Built: Full rewrite of all 12 draw functions using chibi pixel-art proportion principles. FURRED: BIG ROUND HEAD is the focal feature — head 8-wide, ears add 3 rows above; body NARROWER (6-wide) than head to emphasise chibi dome; 4 stub legs; mane crown at final with accent shimmer. WINGED: no ears (smooth dome top), head 6-wide (narrower than furred), eyes HIGH on face (avian), body only 4-wide; wing focal feature grows: shoulder nubs → folded wings in accent colour → fully spread accent wings spanning canvas edges; 2 legs only (bipedal). SCALED: flat-top head (no bumps), side frills protrude sideways, SLIT EYES (1×2 vertical vs 2×2 round), tail IS the focal feature — tiny with no accent at baby → accent tip at teen → large 3-row bold accent block at final. CHITIN: 3-band stacked structure (4→6→8 wide) at all stages; antennae single-pixel thin; compound eyes bulge 1px beyond head width; accent used for wings only — absent at baby, wing buds at teen, full spread wings at final with accent ring on abdomen. Pattern overlays (spots/stripes) updated to match new body layouts.
- Not finished: nothing
- Blockers/risks found: none — visual test recommended in browser with seeds 3=furred, 17=winged, 33=scaled, 49=chitin
- Ready to start next: Phase 4 NPC System (5 NPCs: Prof Owl already wired; add Grandma Turtle, Clover Kid, Crystal Fairy, Sleepy Mole)
- Needs Chatbot decision first: none

**2026-06-20 — creatureAlgorithm.js silhouette redesign (round 3 — Pokémon-proportion):**
- Built: Reverted all stages to 12×12 grid (removed 16×16 final). Redesigned all 12 draw functions using proportion-based differentiation. FURRED: wide quadruped (10-12 wide body, 4-leg pairs with belly gap, triangular ears; baby=stub tail, teen=shoulder mane tufts, final=mane crown). WINGED: narrow bipedal (4 wide body, 2 legs only, beak, tail extending right; baby=tiny wing nubs, teen=folded wing buds, final=wings spread to canvas edges). SCALED: round compact (Squirtle proportion, dorsal ridge above head instead of ears, slit eyes, arm stubs + 2 bottom legs; baby=small frill, teen=head-side frills, final=large dramatic corner frills). CHITIN: 3 stacked segments (head smallest→thorax medium→abdomen largest; antennae always, legs from thorax only; baby=4 legs, teen=6 legs+wing buds, final=full wings+pincers). Draw order fixed: beak and tongue drawn AFTER body so they appear on top.
- Not finished: nothing
- Blockers/risks found: none — visual test needed in browser; test seeds 3=furred, 17=winged, 33=scaled, 49=chitin (small sequential seeds cluster to furred in prng)
- Ready to start next: Phase 4 NPC System (5 NPCs: Prof Owl already wired; add Grandma Turtle, Clover Kid, Crystal Fairy, Sleepy Mole)
- Needs Chatbot decision first: none

**2026-06-20 — creatureAlgorithm.js silhouette redesign (round 2):**
- Built: Fundamentally different architecture per body type. FURRED = 4-legged quadruped stance (4 legs in two pairs with belly gap). WINGED = wings dominate width (canvas-edge to canvas-edge on final), beak, 2 taloned feet, fan tail feathers. SCALED = HORIZONTAL for baby/teen (head left, tail right, frills on left edge, horizontal body — completely unique orientation vs other types); final is standing 2-legged serpent dragon. CHITIN = 3 stacked segments where abdomen is LARGEST (true insect proportion), thin antennae, legs radiate from thorax. Final stage uses 16×16 grid for more detail. Squint test passes — all 4 types readable in silhouette alone.
- Not finished: nothing
- Blockers/risks found: none
- Ready to start next: Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-20 — creatureAlgorithm.js full visual redesign:**
- Built: 4 body types (`furred`/`winged`/`scaled`/`chitin`) × 3 stages = 12 hand-placed silhouettes on 12×12 pixel grid. 20 palette variants across 6 elements (3–4 per element). Pattern overlay system: `none`/`spots`/`stripes` (50%/25%/25%). All driven by `prng(seed)` — 3 consecutive draws: palette index → body type → pattern. `COLORS` → `PALETTES` (array-of-objects per element). `getElement()`, `getCreatureSeed()`, `drawCreature()` signature all unchanged. Visually reviewed in browser — 4 body types clearly distinct (furred=cat ears, winged=wing protrusions+beak, scaled=side frills+tongue, chitin=antennae+segmented). Stage evolution (baby→teen→final) clearly readable.
- Not finished: nothing
- Blockers/risks found: prng(seed) with small sequential seeds (1–16) clusters to furred body type. Real creature seeds are hash-of-name×date so they span 32-bit space — distribution is fine in practice. Test seeds needed to be chosen from different clusters to verify all 4 types.
- Ready to start next: Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-20 — Phase 1.1c — Mystery Adventurers: real procedural creature rendering:**
- Built: `ELEMENT_STATS` reverse-map (6 entries) + `elementToStats(element, evoStage)` — derives placeholder xp/acc/streak values that route `getElement()` to the correct element without touching `creatureAlgorithm.js`. Used option (a) as specified. `CreatureCanvas` component (canvas + useEffect) calls `drawCreature(canvas, Number(seed), elementToStats(...))`. Card: 64×64 canvas replaces emoji placeholder. Modal: 192×192 canvas replaces emoji placeholder. Both still show creature_name text label. No changes to any lib file.
- Not finished: nothing
- Blockers/risks found: creature_seed from Supabase is a bigint — `Number(seed)` coerces it safely for values within 32-bit range (hash function output); no precision loss expected
- Ready to start next: Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-20 — Phase 1.1b — Mystery Adventurers: creature stats + rarity cards:**
- Built: `RARITY` 5-tier color map; `RarityBadge` component (rarity-colored border/bg/glow); `StatBar` component (reuses `.px-hp-bar-outer/.px-hp-bar-inner` CSS, pixel font label, numeric value). `AdventurerModal` (createPortal): opens on "ดูสเตตัส" tap; 72px emoji avatar with rarity border; creature_name + badge; masked display_name; 4 stat bars with fixed max refs (HP/300, ATK/80, DEF/60, SPD/300); "ท้าเล่น" closes modal + fires 3s toast. Card updated: rarity-colored border + optional glow; 48×48 emoji avatar; creature_name + badge + display_name info; "ดูสเตตัส" button. source field intentionally ignored — player/bot cards look identical.
- Not finished: nothing
- Blockers/risks found: stat max reference values (HP 300, ATK 80, DEF 60, SPD 300) are estimates — Chatbot should confirm if bar scaling looks wrong with real data
- Ready to start next: Phase 4 NPC System
- Needs Chatbot decision first: none

**2026-06-20 — Phase 1.1 — Unified Friends screen + Mystery Adventurers tab:**
- Built: FriendsScreen.jsx fully rewritten. Tab bar reduced from 4 → 2 tabs (เพื่อน / ผู้คนอื่นๆ). FriendsTab: single scrollable page loading all data in parallel (Promise.all: friendships + ensure_friend_code + my_friends); pending requests section only renders when requests.length > 0 (no empty state); My Code / Add Friend / Friends list sections always shown. All RPC calls, message logic, and error handling preserved unchanged from Phase 1. MysteryTab: calls get_mystery_adventurers({ p_limit: 8 }) on mount; 8 adventurer cards with display_name + ⚔️ icon (player/bot look identical); "ท้าเล่น" = mock toast for 3s (no backend call); "🔄 สับใหม่" button re-calls RPC. Toast is fixed-positioned at top:60, zIndex:9999.
- Not finished: nothing
- Blockers/risks found: get_mystery_adventurers return shape assumed as array of { display_name, source } — source field intentionally ignored in rendering
- Ready to start next: Phase 4 NPC System
- Needs Chatbot decision first: none

**Status:** Database migration is DONE and deployed to production Supabase project `kidquest`.
**Your job:** Implement the React frontend that calls these existing tables/functions. Do NOT write any SQL or modify the database — it's already live.

---

## What already exists in Supabase (do not recreate)

### Tables

- `public.friend_codes` — `user_id` (PK, FK→auth.users), `code` (text, unique, 6 chars), `created_at`
- `public.friendships` — `id` (PK uuid), `requester_id`, `target_id`, `status` ('pending'|'accepted'|'rejected'), `created_at`, `responded_at`

### View

- `public.my_friends` — already filtered by RLS to the logged-in user. Columns: `friendship_id`, `friend_user_id`, `friend_name`, `created_at`, `responded_at`. Only contains `status = 'accepted'` rows.

### RPC functions (call via `supabase.rpc(...)`)

1. **`ensure_friend_code(p_user_id uuid)`** → returns `text` (the code)
   - Idempotent. Call once right after a user's first egg is created.
   - Generates a permanent 6-char code (charset excludes O/0, I/1, L — kid-safe, no ambiguous chars).

2. **`send_friend_request(p_code text)`** → returns table `(friendship_id uuid, status text)`
   - The ONLY way to look up another user — by exact code match.
   - Handles: code not found (raises exception), self-friend attempt (raises exception), existing pending/accepted relationship (returns existing row instead of duplicating).
   - Auth required (`auth.uid()` used internally as requester).

3. **`respond_friend_request(p_friendship_id uuid, p_accept boolean)`** → returns `text` (new status: 'accepted' or 'rejected')
   - Only callable by the `target_id` of a pending request.
   - Raises exception if not authorized or already resolved.

### Direct table reads (RLS-protected, safe to query directly)

- `select * from friend_codes where user_id = auth.uid()` → get my own code to display
- `select * from friendships where target_id = auth.uid() and status = 'pending'` → incoming requests to show "Accept/Reject" UI for
- `select * from friendships where requester_id = auth.uid() and status = 'pending'` → outgoing requests I'm waiting on (show "Pending..." state)
- `select * from my_friends` → my accepted friends list

---

## UI / UX requirements

**Core safety rule: no browsing, no search, no user list anywhere.** The only way to add a friend is typing in a code given to you outside the app (verbally, or shown by the friend on their own screen).

### Screens to build

1. **My Code screen** (in profile/settings area)
   - Big, friendly display of the user's own code (e.g. large pixel-font text, maybe with a "copy" button)
   - Call `ensure_friend_code` on mount if no code is cached yet, otherwise just read from `friend_codes`
   - Simple flavor text like "Share this code with a friend so they can add you!"

2. **Add Friend screen**
   - 6-character input (auto-uppercase, restrict to the kid-safe charset if you want client-side validation, but server is authoritative)
   - Submit → `send_friend_request(code)`
   - Handle responses:
     - success + status 'pending' → show "Request sent! Waiting for them to accept."
     - success + status 'accepted' (already friends) → show "You're already friends!"
     - error "Code not found" → friendly "Hmm, that code doesn't match anyone. Double-check it!"
     - error "Cannot friend yourself" → friendly "That's your own code, silly!"

3. **Friend Requests screen** (incoming)
   - List rows from `friendships` where `target_id = me` and `status = 'pending'`
   - For each: show requester's name (you'll need to join/fetch `eggs.child_name` for `requester_id`, since `my_friends` view only covers accepted ones — either extend a similar view or do a client-side fetch per row)
   - Accept / Reject buttons → `respond_friend_request(id, true/false)`
   - On accept, refresh the friends list

4. **My Friends screen**
   - Query `my_friends` view directly
   - Show as a simple list/grid with friend names (pixel UI style, consistent with rest of KidQuest)
   - This is also where future features (compare creatures, visit, battle a friend, etc.) would hook in — not in scope for Phase 1, just display for now

### Visual style

Follow existing KidQuest pixel art conventions: Press Start 2P font (English), Sarabun (Thai), hard shadows, zero border-radius, no soft gradients.

---

## Edge cases to handle gracefully

- User has no code yet (new account, first session) → auto-generate via `ensure_friend_code` before showing "Add Friend" or "My Code" screens
- Network/RPC errors → don't crash, show a simple retry-friendly message (this is a kids' app — never show raw error text or stack traces)
- Empty states: no friends yet, no pending requests yet — both need friendly empty-state copy/illustration, not a blank screen

## Explicitly out of scope for this phase

- No friend-vs-friend battles yet
- No chat/messaging between friends
- No "remove friend" UI yet (can be added later — table supports it via a future delete/status policy, not built yet)
- No leaderboards or friend comparison screens yet

---

## Reference: full SQL already deployed

See `friend_system_migration.sql` (already run in Supabase SQL Editor, production). If you need to double check exact column names or constraints, that file is the source of truth — do not re-run it, just reference it.
